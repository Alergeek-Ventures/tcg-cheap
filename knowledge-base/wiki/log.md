# Wiki Log

## [2026-08-10] acquisition health hardening | Passive freshness and terminal repair
- Task completed: Added a strict shared acquisition-health policy, explicit 60-second valuation/exchange worker bounds, and a unique 15-minute operations worker that repairs the oldest at-most-100 attempts still running at or beyond the 15-minute boundary. Reconciliation takes ordered provider advisory locks plus row locks, rechecks eligibility, preserves admitted request counts, records failed/unknown, restores missing health, updates streaks, and is idempotent. A running-only partial index prevents retained terminal history from degrading candidate selection.
- Evidence and admin behavior: Source-health persistence now rejects nullable-status loopholes and contradictory latest terminal chronology. The authenticated operations overview validates policy against configured providers, uses one evidence clock, fails closed on future/impossible health, marks NBP current/stale/no-success at a strict 36-hour boundary, labels unscheduled TCGdex sources on demand, and marks boundary-aged running attempts overdue until repaired. No raw diagnostics enter persistence or UI, and this does not claim active provider probes or automated circuit breaking.
- Validation: Focused operations/worker/admin coverage passes 101 tests; canonical `direnv exec . mix check --verbose` passes 640 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean. Generated migration rollback/up passed, and live PostgreSQL inspection confirmed the partial index plus strict health constraint. Browser validation covered freshness states, overdue evidence, and successful failed/unknown reconciliation; only intentional restart-time LiveView connection errors occurred. Temporary run, health, administrator, and tokens were deleted and verified absent. Final read-only review found no actionable issue.
- Remaining boundary: Terminal stranded-run repair and passive scheduled-source freshness are complete. Active health probes/circuit breaking, acquisition priorities, actual-cost reconciliation, safe manual retry, import issues, production source permission/data, real catalogue/model validation, trusted-proxy attribution, and deployment/runbooks remain unfinished.

## [2026-08-10] Singles catalogue and valuation inspection | Read-only private-safe administration
- Task completed: Expanded the authenticated pinned AshBackpex catalogue with index/show-only `/admin/catalogue/card-sets`, `/admin/catalogue/cards`, and `/admin/catalogue/valuations`. Administrators can inspect imported set identity/coverage, exact printing metadata and embedded Cardmarket mapping status/evidence, and immutable current plus archived single-valuation value/policy/source/metric/timestamps. Card and valuation rows link across exact identities; no create/edit/delete, card-mapping correction, manual refresh, or provider action is exposed.
- Authorization and data safety: `CardSet`, `CardPrinting`, and `SingleValuationSnapshot` generic/admin reads are now strict-admin protected while explicit import/lock/search/exact-ID/bulk and valuation record/history actions remain available. Narrow relationship authorization preserves public card-set/current-valuation and valuation-to-card projections without opening anonymous generic reads. Set/card source payloads, card variant data, and normalized search columns are no longer selected by default and remain `Ash.NotLoaded` unless trusted internal code explicitly selects them; the admin fields never include them. AshBackpex's one-column generic-query limitation is handled with unique TCGdex-ID set/card order, stable non-chronological UUID valuation order, and disabled ordering on non-unique columns.
- Validation: Focused admin coverage passes 4 tests, and the catalogue/singles/Home/CardDetail/Trade regression passes 327 tests. Canonical `direnv exec . mix check --verbose` passes 618 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean. Desktop and 390px browser checks covered card and valuation ledgers, both current/archive rows, internal table scrolling without page overflow, and no console warning/error. The Impeccable detector returned no findings; temporary set/card/valuation/admin/token rows and screenshots were removed, and final read-only review was clean.
- Remaining boundary: This is inspection of locally retained evidence, not a separate auditable card-external-mapping resource, card-mapping correction/history, production catalogue/mapping evidence, import-issue tracking, safe manual refresh/retry, buying-model inspection, automated stale-source/circuit-breaker operations, real-data validation, or deployment completion.

## [2026-08-10] mapping catalogue and correction history | Reasoned reversible decisions
- Task completed: Expanded the authenticated catalogue with `/admin/catalogue/mappings` and `/admin/catalogue/mapping-history`. Administrators can inspect every mapping and newest-first immutable safe decision snapshot without retained provider payloads. Pending/review approval or rejection stays in `/admin/review`; matched/rejected rows expose a separate reasoned confirmation that reopens the row into review, preserving matched evidence as the next candidate and retaining prior decisions.
- Correctness and retention: `ListingProductMappingDecision` records created/baseline/approved/rejected/reopened state, product/confidence/reason, allowlisted evidence method/GTIN, mapping version, and system or administrator attribution in the parent transaction. Direct authorized history fabrication is forbidden, history failure rolls back the mapping transition, mapping/product references restrict deletion, import writes only a missing initial snapshot, and a separate migration baselines existing rows. Approval/rejection/reopen now require the supplied record and locked latest row to match the displayed version, closing mixed stale-record/current-token transitions. Blank correction reasons, nonterminal reopens, malformed optional evidence, stale callers, and destructive admin actions fail safely.
- Validation: Focused mapping history/catalogue plus review coverage passes 23 tests. Canonical `direnv exec . mix check --verbose` passes 614 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean. Generated schema migration plus baseline data migration rollback/up passed; a real rejected-row baseline was verified and removed. Desktop and 390px browser checks covered authentication, chronological mapping/history navigation, blank and successful correction, review-queue return, 45px actions, no horizontal page overflow, and no console warnings/errors. Temporary rows, administrator, tokens, and browser session were deleted and verified absent; final read-only review was clean after chronological admin ordering was corrected.
- Remaining boundary: This is local correction capability and auditability, not production mapping evidence. Written source permission, real catalogue/listing/mapping/model validation, cards/external mappings, valuation snapshots, import issues, safe manual refresh/retry, model inspection, remaining operations automation, and deployment stay unfinished.

## [2026-08-10] sealed alias catalogue | Manual pending curation without evidence overwrite
- Task completed: Expanded the authenticated pinned AshBackpex catalogue with `/admin/catalogue/aliases`. Administrators can list/show every sealed-product name/EAN alias, create normalized pending manual aliases against a typeahead-selected canonical product, and revise only manual pending rows. Provider-backed pending aliases and approved/rejected rows remain inspectable but read-only; approval/rejection remains the consequential `/admin/review` workflow. Delete/bulk-delete and source-payload display are absent.
- Correctness and authorization: Generic alias reads used by AshBackpex list/get/count are strict-admin protected. Public sealed search now traverses a filtered approved-name relationship instead of opening the full alias relationship. Manual revision requires an administrator, injects the displayed `updated_at`, locks/re-reads the latest pending row, and rejects source, source-reference, or retained-payload evidence before write. Normalization and GS1 validation rerun on create/edit; stale forms, invalid GTINs, provider evidence, and reviewed states cannot be overwritten.
- Validation: Focused alias domain/public-search/AshBackpex coverage passes 39 tests. Canonical `direnv exec . mix check --verbose` passes 595 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean; final read-only review is clean. Desktop and 390px browser checks covered sign-in, list/new creation, typeahead product selection, normalized pending evidence, provider read-only behavior, hidden payload, internal table scrolling without page overflow, generated 44px actions, and no console warning/error. The Impeccable detector returned no findings. Temporary aliases, product, administrator, tokens, and session state were deleted and verified absent.
- Remaining boundary: This is local curation capability, not production catalogue evidence. Written source permission, real alias/EAN/MSRP curation, mapping catalogue/correction history, cards/external mappings, valuation snapshots, import issues, safe manual refresh, model inspection, real market validation, and deployment remain unfinished.

## [2026-08-10] acquisition observability | Durable safe attempts and source streaks
- Task completed: Added `AcquisitionRun` and `SourceHealth` AshPostgres resources plus generated migration/snapshot sets `20260810004856`–`20260810013150`. Every TCGdex Cardmarket valuation, NBP exchange-rate, and configured sealed-retailer worker attempt now retains canonical identity, worker/queue/job attempt, running/succeeded/retryable-failure/failed/cancelled outcome, one fixed secret-free category, admitted request count, and timestamps. Provider health retains last start/success/failure/outcome/category and a serialized consecutive-failure streak. Valid local-card/retailer preflight cancellations are visible without provider invocation; malformed jobs without safe canonical identity remain outside the ledger.
- Correctness and request accounting: Same persisted job/attempt execution is single-use, a normal callback result is not reported when durable finalization fails, final Oban errors are terminal `failed`, and a later attempt atomically marks lower still-running attempts for the same job/provider failed/unknown. Provider advisory locking serializes starts/completions and timestamps new attempts after lock acquisition; concurrent streak updates do not get lost. Successful request counts increment only after budget reservation. TCGdex catalogue, TCGdex Cardmarket, NBP, and LootQuest metered Req paths all disable redirects and internal retries, so one admission cannot silently cover a redirect chain. Migration rollback converts terminal status safely before restoring old constraints; rollback/up was exercised with a real failed run.
- Admin behavior: The existing operations-desk visual benchmark and responsive docket system were reused rather than introducing a new layout language. Configured provider dockets now show last completed outcome, last success, failure streak, and safe category. A bounded stream renders 25 recent configured-provider acquisition attempts by default, 50 maximum, without arguments, payloads, raw diagnostics, or exception text. Desktop 1000px and mobile 390px browser checks showed one real safe failure projection, 45px navigation/actions, one-column mobile reflow, and no horizontal overflow or current-page console warning/error. Temporary run, health, administrator, token, and session data were deleted and verified absent.
- Validation and remaining boundary: 124 focused operations/worker/adapter/LiveView tests passed during integration. Canonical `direnv exec . mix check --verbose` passes 588 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean; final read-only review found no actionable issue. This is structured lifecycle evidence, not actual-cost reconciliation, stale-source policy, automated health checks/circuit breaking, import-issue tracking, safe manual retry, or complete local-job observability. A killed terminal attempt with no later persisted retry may remain running until a future periodic reconciler exists.

## [2026-08-10] homepage discovery benchmark | Exact identity before quiet exploration
- Benchmark recorded before implementation: The official [Pokémon TCG Live variant flow](https://support.pokemon.com/hc/en-us/articles/45762566240788-How-to-View-Card-Variants-in-Pok%C3%A9mon-TCG-Live) makes a player search for and select a card before exposing its variations and rarities, preserving exact identity at the consequential step. The official [MTG Arena mobile deck-building reference](https://magic.wizards.com/en/news/mtg-arena/mtg-arena-state-game-january-2021-01-21) keeps search and advanced filters available on mobile while adapting collection browsing to touch and a compact two-row/list composition. TCG Cheap should therefore keep category-specific search dominant, show cross-category matches only as a clearly separated recovery path, and place a small amount of real local discovery evidence below the primary task without copying game-client chrome or mixing identities.
- Task completed: Idle Home now renders up to four real local **Biggest changes** singles and four **Recent sealed releases**, omitting either ledger when it has no trustworthy rows. Singles use typed exact-card projections from the fixed preceding 30 UTC dates: last successful snapshot per day, at least two points spanning seven days, active `tcgdex_cardmarket_v1` current-latest evidence, and at least 5% absolute change. Rows show exact identity, current EUR, signed movement, elapsed days, freshness/stale state, canonical low WebP or honest fallback, and a direct route. Sealed releases are approved official PL/en current/discontinued products inside a fixed inclusive 180-date window. The retained snapshot scan has a generated `(policy_version, fetched_at, card_printing_id)` index.
- Search and accessibility: A zero-result primary search now makes at most one bounded local opposite-category query and renders up to four direct links in a clearly separate `Sealed products instead` or `Singles instead` ledger outside the combobox/listbox. Primary `aria-expanded` remains false, fallback availability is included in the persistent polite search summary, and mode/short/idle/error/success/Escape transitions clear recovery streams. Discovery containers remain stream-mounted but semantically hidden while search is active, so their rows return without storing regular collection lists or re-querying. No provider call or fabricated discovery row enters Home.
- Validation: Focused discovery, freshness, and Home LiveView coverage passes 34 tests, including UTC same-day closes, inclusive 30-date bounds, evidence thresholds, release eligibility/window/order/limit, stream lifetime, both fallback directions, persistent announcements, and clearing. Canonical `direnv exec . mix check --verbose` passes 571 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean; assets build also passes. Desktop 1000px and mobile 390px browser checks show even discovery rows, two-to-one-column reflow, no horizontal overflow, 45px mode controls, 67px-plus discovery links, both recovery directions outside primary listboxes, `aria-expanded=false`, and no console warning/error. Temporary browser cards, valuations, and sealed products were deleted and verified absent. The detector retained the two known square-border false positives plus existing design-system advisories; no new structural warning remained after the missing-image row was corrected.
- Remaining boundaries: This populates only from accumulated real local rows and makes no production catalogue/history claim. Strong-versus-weak cross-category fallback and ranking remain part of real collision tuning; the current fallback is deliberately zero-match-only. Production source permission/data, production-scale discovery caching/measurement, real 30-day/model validation, remaining admin/source-health work, and deployment are still incomplete.

## [2026-08-10] retailer catalogue expansion | Stale-safe controls and read-only listing evidence
- Task completed: Expanded the authenticated pinned AshBackpex catalogue from sealed products to `/admin/catalogue/retailers` and `/admin/catalogue/listings`. Administrators can list/show/create curated retailer identities and stale-safely edit name, slug, regular-retailer/LGS category, homepage, and active/disabled state; the provider source key is immutable after creation, and metadata/source payloads never enter the configured fields. Current retailer listings expose retailer, source identity, GTIN, PLN price/currency, stock/projection state, direct URL, and first/last/check timestamps through list/show only. Listing create/edit/delete remain absent because provider ingest and immutable observations remain the only writers.
- Correctness and authorization: Retailer updates inject the displayed `updated_at`, lock the latest row inside the action transaction, and reject stale forms. Primary generic plus admin reads are strict-admin protected because AshBackpex 0.1.12 performs list/get/count through generic queries even when a read action is configured. Narrow `accessing_from` bypasses preserve relationship traversal needed by existing matched public offers and local aggregate jobs; direct anonymous generic reads remain forbidden. Existing register/enable/disable/lookup/active and listing ingest/disable/lookup actions remain actorless operational boundaries. Because the current adapter applies one UI sort, retailer pagination uses unique slug order and listing pagination uses stable UUID order instead of claiming a nondeterministic newest-first order.
- Validation: Focused retailer/product/domain coverage plus public sealed/detail/aggregate regressions passed 65 tests. Canonical `direnv exec . mix check --verbose` passed 562 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean; assets build passed and final code review was clean after correcting relationship authorization and generic-query pagination. Desktop and 390px browser checks covered navigation, listing evidence, a real category/status save, immutable source identity, no page overflow with internal table scrolling, generated controls/row links at the 44px class, and no current-page console warning/error. The detector retained the two known square-border false positives plus existing design-system advisories; no finding points at the admin additions.
- Remaining boundaries: This does not add production retailers/listings or enable the disabled LootQuest adapter. Alias CRUD, cards/external mappings, valuation snapshots, import issues, structured source health/staleness, safe manual refresh/retry, buying-model inspection, actual-cost reconciliation, production sealed permission/data, real validation, and deployment/runbooks remain unfinished.

## [2026-08-10] initial AshBackpex catalogue | Authenticated sealed draft CRUD
- Benchmark: The previously recorded official [Pokémon TCG Live variant flow](https://support.pokemon.com/hc/en-us/articles/45762566240788-How-to-View-Card-Variants-in-Pok%C3%A9mon-TCG-Live) keeps exact identity explicit before a consequential action, while the official [MTG Arena mobile deck-builder reference](https://magic.wizards.com/en/news/mtg-arena/state-game-adventures-forgotten-realms-2021-07-02) keeps controls directly reachable and reflows information on phones. This internal catalogue therefore keeps product identity/evidence grouped, makes list/show/create/edit direct, preserves review as a separate consequential step, and scopes generated controls to 44px without importing game-client ornament.
- Task completed: Independently pinned AshBackpex `0.1.12` and Backpex `0.19.6`, including required hooks, Tailwind sources, PubSub/translation configuration, cookie/resource routes, and a Backpex admin shell integrated with the existing administrator session. Authenticated `/admin/catalogue/products` now lists/shows sealed products, creates manual drafts, and revises drafts through the existing stale-safe row-lock/version action. Approved products remain inspectable but do not offer draft editing; provider payload/sync fields and all destructive actions are excluded. Review approval/archive plus alias/mapping decisions remain in `/admin/review`, and `/admin/operations` remains focused on provider controls.
- Security and correctness: The existing `current_admin` is mirrored only inside the required admin on-mount hook to AshBackpex's `current_user` actor contract. Admin-specific create/catalogue actions are strict-policy protected, and because AshBackpex 0.1.12 executes list/get/count through the primary read rather than its configured read action, that actual generic read is also strict-admin protected; explicit public sealed lookup/search actions remain separate. The edit projection loads record version plus state required by global validation, injects the displayed version into `revise_draft`, and stale concurrent review wins. Bulk delete is absent despite the upstream actor limitation.
- Validation: Canonical `direnv exec . mix check --verbose` passed 557 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean. Focused sealed catalogue/review/operations coverage passed 40 tests; adjacent public sealed/pricing/admin coverage passed 111; assets build passed; final review was clean. Browser validation at desktop and 390px covered authentication, list, mobile edit/save, internal table scrolling with no page overflow, and scoped 44px controls. One development-only Phoenix live-reload iframe read timeout appeared after an idle session; no application interaction failed. Temporary product/admin/token rows were removed and verified zero.
- Remaining boundaries: This is the first main AshBackpex resource, not broad admin completion. Retailers/listings, aliases outside the review queue, cards/mappings, valuation snapshots, import issues, source health/staleness, manual refresh/retry, buying-model inspection, other job operations, production sealed permission/data, real validation, and deployment/runbooks remain unfinished. AshBackpex/Backpex are pre-1.0; upgrades require an explicit compatibility check.

## [2026-08-10] authenticated operations ledger | Bounded provider controls and failure visibility
- Benchmark: The official [Pokémon TCG Live variant flow](https://support.pokemon.com/hc/en-us/articles/45762566240788-How-to-View-Card-Variants-in-Pok%C3%A9mon-TCG-Live) keeps exact identity explicit before a consequential action, while the official [MTG Arena mobile deck-builder reference](https://magic.wizards.com/en/news/mtg-arena/state-game-adventures-forgotten-realms-2021-07-02) keeps direct controls touch-sized and reflows information for mobile. The internal operations desk is not a game-client surface, so it borrows those decision principles rather than ornament: provider identity, state, counters, and one reversible action stay together; ledgers collapse to one column at phone width.
- Task completed: Added authenticated `/admin/operations` beside the sealed review desk. The bounded overview merges at most 100 server-configured providers with their current UTC hour/day/month counters, aggregates global current-window usage in SQL, and exposes at most 25 retained retryable/discarded/cancelled jobs by default (hard maximum 50). Job arguments, metadata, and raw exception text never enter the LiveView; only worker/queue/state/attempt/time and a conservative failure category are projected. The page explicitly states that spend is an estimated pre-HTTP reservation and that actual-cost reconciliation/source health remain absent.
- Control correctness: Configured provider kill switches are admin-authorized and use the displayed `updated_at` as a transaction-local row-lock version. Same-version concurrent transitions permit one update; admission-versus-disable races linearize so no request is admitted after disable wins. Unchanged acquisition-registration upserts no longer churn the control version, while real configured-limit changes still update it. An unpersisted configured provider can be disabled safely on first control. Provider/window reads are configuration-bounded, global totals still include current usage from retired configuration entries, and malformed configuration fails the entire desk closed rather than presenting a false empty state.
- Validation: Focused operations plus adjacent metered-worker coverage passed 76 tests. Canonical `direnv exec . mix check --verbose` passed 550 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean; final code review was clean. Browser validation at 1000px and 390px showed the global ledger, three provider dockets, honest empty jobs state, no horizontal overflow, and all provider/sign-out controls above 44px; NBP disable/re-enable restored active state with no console warning/error. The detector retained the two known square-border false positives plus existing design-system advisories. Temporary screenshots, metrics, administrator, and session state were removed.
- Remaining boundaries: This is a focused custom operational LiveView, not completion of the required AshBackpex CRUD surface. Actual paid-cost reconciliation, structured acquisition runs/import issues, source-health freshness/failure streaks, automated circuit breakers, retailer-adapter controls, safe manual refresh, model-configuration inspection, broader job observability/retry operations, production sealed access/data, real validation, and deployment/runbook work remain unfinished.

## [2026-08-09] public acquisition throttle | Direct-peer fail-closed admission
- Task completed: Implemented the next unblocked operations-safety slice while production sealed permission/data remains blocked. `TcgCheapWeb.PublicAcquisitionLimiter` is a supervised bounded single-node fixed-window limiter with a configurable default of 30 acquisition candidates per validated direct IPv4/IPv6 peer per hour, 10,000 entries, minute pruning, exact concurrent serialization, expiry reclamation, and fail-closed invalid-peer/capacity behavior. LiveView websocket and longpoll connect info now include `:peer_data`; CardDetail and Trade retain only the direct peer address and deliberately ignore untrusted forwarded headers.
- Acquisition correctness: `ValuationAcquisition` and `ExchangeRateAcquisition` now accept strictly validated zero-arity public admitters and require one before stale/missing work can enqueue; omitted, malformed, raising, throwing, or exiting admitters fail closed. Fresh singles and today's NBP cache consume no peer quota. Card detail reserves once immediately before stale/missing valuation insert. Trade reserves independently for every canonical stale/missing card, allowing honest partial bulk results without a 100-card URL bypass, and once for stale/missing public NBP work. Rejection queues no denied job and retains cached/`?`/PLN-unavailable states. Cron/manual direct enqueue remains available and every eventual HTTP request still requires the persisted provider/global budget admission.
- Validation: Focused limiter, valuation/exchange acquisition and worker, CardDetail, and Trade coverage passed 81 tests. Canonical `direnv exec . mix check --verbose` passed 533 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean. Final code review found no remaining actionable blocker or warning. The behavior reuses existing public states and changes no layout, so no new browser visual claim is made.
- Remaining boundaries: The peer limiter is one-VPS in-memory abuse smoothing and resets on application restart; PostgreSQL provider/global hourly/daily/monthly and spend controls remain the durable hard cap. A future reverse proxy needs explicitly trusted client-address resolution or users will share the proxy's direct-peer bucket; Caddy integration remains deferred. Actual-cost reconciliation, acquisition priorities, automated source health/circuit breaking, authenticated usage/failure views, AshBackpex CRUD/operations, production sealed access/data, and real model validation remain unfinished.

## [2026-08-09] public sealed guide/graph slice | Persisted local projection
- Task completed: Documented the completed public `/sealed/:slug` guide/history slice. The route consumes persisted current-model guide snapshots locally and never recomputes the model or calls providers. `SealedBuyingGuidePublicProjection` validates expected product binding, exact source aggregate and preceding-history fingerprints, source-relative history, and time/version/currency/state/money/factor/trend invariants; corruption, mismatch, or read failure fails closed. Exact ready guides render four inclusive-ceiling Great/Fair/Expensive/Avoid ranges and plain-English explanations from persisted factors. Exact Limited guides render collector-readable Limited data reasons/factors; older-ready and newest-limited/older-ready combinations remain explicitly previous/cached/outdated, never current.
- Graph and correctness: `SealedDailyAggregatePublic` centralizes defensive public aggregate validation. `SealedMarketHistory` renders a fixed 30 UTC-calendar-day accessible SVG with benchmark line, typical-range band, missing-day gaps, no controls/interpolation, and a textual daily ledger. Only canonical ready rows for the expected product are plotted. Phase 5 public rendering/graph/plain-English explanation items are implemented in code, awaiting real Polish data/model validation. Phase 4 remains incomplete only for absent production catalogue/retailer evidence and real validation, not absent guide/graph UI.
- Validation and cleanup: Canonical `direnv exec . mix check --verbose` passed 523 tests; the four focused new/updated public aggregate/guide/history/LiveView files passed 29 tests; assets build passed; final code review was clean. Browser checks at desktop 1000px and mobile 390px showed ready bands/reasons/graph/ledger, no horizontal overflow, sampled controls at least 44px, and no console warnings/errors. Impeccable showed the two known square-border false positives plus design-system advisories; graph colors align with existing tokens. Temporary browser fixture rows were deleted and counts verified zero.
- Remaining boundaries: No production SKU/retailer/listing/aggregate/guide, provider permission, live schedule, or fabricated production history was added, and no production-data claim is made. Written source access, real catalogue/observations/model validation, AshBackpex/operations, actual-cost/per-IP/source-health/admin controls, homepage discovery/tuning/deployment, and the broader MVP acceptance work remain.

## [2026-08-09] persisted sealed buying guides | Exact-revision local recomputation
- Task completed: Implemented the next unblocked Phase 5 integration slice. `SealedDailyAggregate` now retains snapshot-time MSRP, explicit mapping confidence, and policy-relevant embedded source evidence; a canonical scalar SHA-256 identifies the exact aggregate revision independent of evidence order, Decimal scale, and DateTime display precision. `SealedBuyingGuideSnapshot` persists one product/date/model-version output with ready/Limited state, reference and three ceilings, confidence, trend/change, availability, component centers, ordered explanation factors, source aggregate identity/time/fingerprint, a canonical fingerprint of the exact preceding 30-day history revisions, restrictive references, latest/latest-ready/history reads, and app/database decision-table constraints.
- Recompute correctness: Daily aggregate persistence and guide enqueue now share one repository transaction, so enqueue failure rolls back the aggregate. The unique `sealed_buying_guides` worker re-reads and fingerprints both the exact source revision and the strictly preceding 30-day history before reconstructing retained evidence and invoking the pure model. Snapshot persistence locks and rechecks those source rows, so a read/calculate/write race cannot commit mismatched output; stale jobs enqueue the latest revision. Updating a historical aggregate atomically enqueues recomputation for every persisted guide date through `source date + 30 days`, closing downstream invalidation while preserving latest-revision daily rows. Aggregate-time MSRP remains frozen; mappings approved after the aggregate clock are excluded; malformed mapping times fail closed; coherent empty relevant evidence can still produce honest Limited data. Intra-day append-only aggregate history is not claimed.
- Schema and validation: Generated migration/snapshot sets `20260809163539`–`20260809185311` create guide storage, add aggregate evidence/MSRP/mapping-confidence sources, harden trend/time/current-plus-history fingerprint constraints, and index the restrictive source-aggregate foreign key. Focused guide/aggregate integration passes 35 tests; all pricing coverage passes 209 tests. Canonical `direnv exec . mix check --verbose` passes 503 tests with Ash codegen, Sobelow, format, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean. The final review found no code blocker after the mutable-history identity and downstream invalidation findings were corrected. Development migration `20260809185310` applied successfully with the required non-null history fingerprint and zero guide rows. This backend-only batch changes no public UI and makes no browser claim.
- Boundary and next work: No provider request, production SKU/retailer/listing/aggregate/guide, fabricated history, or live-source permission claim was added. Written source permission and real Polish evidence still block production acquisition/weight validation. The next unblocked playability slice is public consumption of persisted ready/Limited guides with plain-English factors, followed by the fixed 30-day benchmark/range graph; broader AshBackpex/operations requirements remain.

## [2026-08-09] sealed buying model v1 | Deterministic pure four-band policy
- Task completed: With written source permission and real Polish sealed data still blocking production acquisition/validation, implemented the pure Phase 5 enabling slice `TcgCheap.Pricing.SealedBuyingModel`. Version `sealed_buying_model_v1` consumes one coherent `sealed_market_daily_v1` aggregate, 30-day aggregate history, optional MSRP, exact snapshot-aligned per-retailer LGS evidence, latest-per-retailer sold-out price evidence, and explicit mapping confidence. It exposes fixed component/confidence weights, inclusive freshness and sold-out windows, trend/availability thresholds, hard ready requirements, limited-reason precedence, and deterministic Great/Fair/Expensive/Avoid intervals. No provider, database, worker, or public request path is involved.
- Correctness and trade-offs: All arithmetic runs under an internal decimal128 precision-34 half-up context, so a caller's process-local Decimal context cannot alter output and is restored afterward. Wrong versions, future/incoherent timestamps, nonfinite prices, duplicate/mismatched evidence, contradictory sold-out prices, impossible source counts, and malformed aggregate state fail explicitly. Coherent weak evidence returns Limited data for uncertain mapping, limited aggregate, stale evidence, insufficient history, low confidence, or a rounded-boundary collision. Optional reference weights renormalize; one recent sold-out row per retailer receives 1.00 weight through 14 days or 0.35 through 30 days; LGS evidence remains separate at 0.10 weight; regular benchmark outlier handling stays delegated to `sealed_market_daily_v1`. The new [Sealed Buying Model v1 ADR](architecture/sealed-buying-model-v1.md) records exact weights, boundaries, synthetic outputs, and provisional trade-offs.
- Validation: The focused model suite passes 37 tests, all pricing coverage passes 179 tests, and canonical `direnv exec . mix check --verbose` passes 473 tests with Ash codegen, Sobelow, format, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean. Coverage includes caller-context independence/restoration; exact 7/14/30-day and history-window boundaries; hard-reason precedence; LGS/sold-out deduplication and count consistency; nonfinite/malformed/future data; all optional centers; exact confidence/reference/band outputs for below/around MSRP, scarce/rising, and falling cases; upstream cheap/expensive outliers; new-release/one-shop/sporadic limited states; interval inclusivity; and rounded-boundary fail-closed behavior. Final code re-review found no Decimal determinism blocker; deterministic wiki metadata/index/internal-plus-raw link checks pass with all five articles indexed; this pure batch has no UI/browser change.
- Boundary and next work: Synthetic fixtures satisfy the pre-real-data case matrix but do not validate the provisional weights against the Polish market. No `SealedBuyingGuideSnapshot`, recomputation job, persisted/public band, plain-English explanation, 30-day sealed graph, production source/SKU/listing/aggregate, or fabricated history was added. Written source permission plus real catalogue/market evidence remains the production blocker; the next unblocked integration step is persisted versioned buying-guide snapshots/recomputation before public bands and graph work.

## [2026-08-09] daily sealed aggregates | Versioned benchmark and honest public snapshot
- Task completed: With written production retailer permission/data still blocking the highest Phase 4 production step, implemented the local-only Phase 5 enabling slice. `SealedDailyAggregate` retains one product/date/calculation-version row with current/limited state, finite ordered PLN benchmark/range, regular/LGS/sold-out/stale/source counts, evidence/calculation times, versioned history/latest-ready reads, newer same-day upserts, and stale-write rejection. Generated migration/snapshot sets `20260809140042`–`20260809145258` create storage and harden ready/limited-reason, finite-money, coherent coverage-count, timestamp, identity, and restrictive-product invariants.
- Calculation and scheduling: Pure `sealed_market_daily_v1` validates loaded evidence, rejects retailer-category contradictions, filters freshness before selecting one cheapest current listing per retailer, requires five fresh regular retailers, keeps LGS separate, deduplicates one latest eligible sold-out listing per retailer into inclusive 0–14/15–30-day buckets, removes Tukey 1.5-IQR outliers, and stores the half-up two-decimal median plus inlier min/max. A local-only unique `sealed_aggregates` Oban job runs daily at 16:00 UTC after NBP, reads all approved products/matched active listings, skips products without mapped evidence rather than fabricating history, and performs no provider request or retailer refresh.
- Public behavior and correctness: Sealed detail now has a neutral `Market snapshot` region. Valid ready data shows current/cached benchmark, typical range, fresh source counts, aggregate date, exact evidence time, and concise methodology; currentness requires a today/yesterday aggregate plus nonfuture evidence within seven days. Empty/weak states visibly say **Limited data**. A newest limited result preserves the independently bounded latest older ready snapshot as explicitly cached/outdated even beyond 30 days, while invalid/unreadable data fails closed. Review-found stale-cheap duplicate masking, sold-out duplicate inflation, contradictory source coverage, misleading always-Limited heading, stale usable snapshot loss, and UI complexity were corrected. The existing warm square surface reflows at 1008px/390px without horizontal overflow, with a 44px-plus methodology summary control.
- Validation and boundary: Aggregate plus adjacent Cron coverage passed 58 focused tests. Canonical `direnv exec . mix check --verbose` passed 436 tests with Ash codegen, Sobelow, format, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean (0 errors). Final review reported no blocker or warning; deterministic wiki metadata/index/internal-plus-raw link lint and `git diff --check` passed. Browser validation covered current benchmark/range/count/date/evidence/methodology at desktop and mobile with no current-page console warning/error; temporary product/aggregate rows were deleted and verified zero. No production SKU/retailer/listing/observation/aggregate, live retailer adapter, fabricated history, 30-day sealed graph, buying model/four bands, or real-market validation is claimed.

## [2026-08-09] request-level acquisition budgets | Concurrent hard-cap foundation
- Task completed: Added the `TcgCheap.Operations` Ash domain with persisted `DataProvider` status/cost/limit configuration and `BudgetUsage` UTC hour/day/month request plus estimated-spend counters. Added strict complete runtime configuration for TCGdex catalogue, TCGdex Cardmarket, and NBP; one serialized PostgreSQL admission transaction now synchronizes configuration without reviving a disabled provider, locks status, checks provider hourly/daily/monthly requests and monthly spend plus global hourly/daily requests and monthly spend, and atomically reserves all three windows before external work. Generated migration/snapshot sets `20260809023642`–`20260809034324` add constrained provider/counter storage, provider-window uniqueness, coherent request-limit ordering, and global-window lookup support.
- Request correctness: Moved accounting from one worker callback to each actual outbound request. Operational TCGdex catalogue `Importer`/`Sync`/`Enrichment`, TCGdex Cardmarket valuations, NBP exchange rates, and configured sealed-retailer refreshes inject a fail-closed request admitter; adapters invoke it immediately before HTTP, LootQuest does so for every page, and metered adapters force internal Req retries off so every later HTTP retry needs another admission. Rejection/disabled/invalid wiring stops before provider code or HTTP; budget persistence failures remain retryable in workers. UTC hour/day windows explicitly zero microseconds, closing a review-found counter-fragmentation bypass, and deterministic per-window provider/global precedence is covered. Catalogue and worker/provider contract tests prove rejection-before-HTTP, one-attempt retry behavior, per-page metering, no partial sealed persistence, and stale/`?` fallback-compatible failure semantics.
- Boundary: Active providers remain `$0`; accounting conservatively records configured estimated cost before the request. This batch does not claim actual paid-cost reconciliation, public per-IP acquisition throttling, priority classes, automated source health/circuit breaking beyond persisted kill switches, AshBackpex/admin usage and failure inspection, production sealed permission/data, or overall MVP completion.
- Validation: 141 focused operations/provider/worker/catalogue tests passed during integration. Canonical `direnv exec . mix check --verbose` passed 406 tests with formatting, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean (0 errors). Final request-metering code review found the microsecond-window bypass, which was corrected with a dedicated regression before this validation. `git diff --check` and deterministic wiki metadata/index/link lint passed.

## [2026-08-09] public sealed comparison foundation | Local approved evidence, honest Limited data
- Benchmark: The official [Pokémon TCG Live variant flow](https://support.pokemon.com/hc/en-us/articles/45762566240788-How-to-View-Card-Variants-in-Pok%C3%A9mon-TCG-Live) reinforces selecting an exact product identity before acting, while the official [MTG Arena mobile deck-builder reference](https://magic.wizards.com/en/news/mtg-arena/state-game-adventures-forgotten-realms-2021-07-02) reinforces direct touch-sized actions and information that reflows rather than shrinking on phones. Neither is a physical sealed-market price reference. The public Sealed slice therefore extends the existing warm valuation bench with canonical product identity first, one local selection action, compact PLN offer evidence, and separate unavailable/sold-out/limited-data states rather than game-client ornament or invented confidence.
- Task completed: Added local PostgreSQL public sealed search over approved released official PL/en canonical names and approved name aliases, with bounded deterministic exact/alias/prefix/similarity ranking, concurrent GIN trigram indexes, stable streamed combobox options, cross-mode tamper rejection, and exact `/sealed/:slug` navigation. The detail route keeps approved identity, release/distribution state, optional provenance-backed MSRP, and matched active-retailer evidence local; it selects one cheapest in-stock listing per shop and one most recently checked sold-out listing per shop within 30 days, sorts them deterministically, keeps sold-out offers separate, and shows category, PLN, stock, exact UTC check time, safe direct link, shipping exclusion, local not-found/read-error states, and **Limited data** instead of inventing a benchmark, graph, confidence, or buying bands. No provider HTTP runs in public paths.
- Correctness: Public actions exclude drafts, future releases, unofficial/non-PL/non-English products, inactive listings/retailers, unmatched mappings, and EAN aliases. Shared autocomplete Enter now carries the current input and clears pending debounce; Home and Trade reject stale cached selections, preventing rapid input from opening an old result. Sold-out ordering uses chronological `DateTime.compare/2`, and offer-read failure no longer claims confirmed empty datasets. Generated migration/snapshots `20260809003932`–`20260809003934` were produced through Ash codegen and reviewed. No production SKU, retailer, listing, observation, source permission, market aggregate, or fabricated history was added.
- Files changed: sealed-product/alias search indexes/actions/preparation, public mapping read/projection, Core interfaces, Home/Trade shared autocomplete handling, sealed detail route/LiveView, scoped CSS, generated migration/snapshots, focused catalogue/Home/Trade/detail tests, current north-star/architecture/provider/index articles, and this log.
- Validation: 63 focused public lookup/Home/Trade/detail tests passed. Canonical `direnv exec . mix check --verbose` passed 378 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean; final code review, deterministic wiki metadata/index/link lint, and `git diff --check` were clean. Browser validation at 1008px and 390px passed character-by-character focus retention, one selected keyboard option, exact navigation, cheapest-per-shop and current/sold-out evidence, long retailer names, Limited data/MSRP/shipping/not-found states, at least 44px controls, no horizontal overflow, and zero console warnings/errors. Temporary product/retailer/listing/mapping/observation records were deleted and verified absent. Next: obtain written source permission and curate real official Polish-English SKU/alias/MSRP/retailer evidence; AshBackpex operations, daily aggregates, 30-day graph, buying model/bands, and real-data validation remain incomplete.

## [2026-08-09] authenticated sealed review desk | Stale-safe admin acceptance
- Benchmark and UI decision: The official [Pokémon TCG Live variant flow](https://support.pokemon.com/hc/en-us/articles/45762566240788-How-to-View-Card-Variants-in-Pok%C3%A9mon-TCG-Live) reinforces confirming exact identity before acting, while the official [MTG Arena mobile deck-builder reference](https://magic.wizards.com/en/news/mtg-arena/state-game-adventures-forgotten-realms-2021-07-02) reinforces direct, touch-sized actions and mobile-first reflow. The internal desk therefore keeps canonical identity and source evidence in one paper docket, uses named one-row decisions rather than bulk dashboard controls, and stacks every form/action at phone width without borrowing consumer-game ornament.
- Task completed: Added a separate `TcgCheap.Accounts` Ash domain with provision-only Argon2 administrator password identity, persisted/revocable tokens, production signing-secret validation, session renewal/logout socket disconnect, and no public registration. Added dual remote-IP plus normalized-email fixed-window sign-in throttling with bounded keys, capped state, periodic expiry, and fail-closed overload behavior. The authenticated `/admin/review` LiveView streams draft products, pending aliases, and pending/review listing mappings with loaded retailer/product evidence; supports draft correction/approval/archive, alias approval/rejection, and mapping confirmation/reasoned rejection; and exposes explicit empty states, queue counts, sign-out, responsive forms, and stable test IDs. Broader AshBackpex CRUD/operations remains required and intentionally unclaimed.
- Correctness and security: Admin-only Ash policies protect human review transitions and queue reads independently of route authentication. Every review form/decision carries the displayed `updated_at`; transaction-local row locks re-read and compare it before mutation, so stale edits or newly changed evidence cannot be silently overwritten, approved, archived, or rejected. Password/email inputs are bounded before Argon2 work, sessions are renewed, logout revokes stored tokens, authentication responses remain generic, and no fixture or production sealed data was added. Generated auth migrations/snapshots install `citext`; the generated extension rollback was narrowly corrected because it would otherwise drop shared pre-existing Ash functions.
- Validation: Focused administrator/catalogue coverage passed 56 tests, including authentication/policy boundaries, password bounds, dual-key/cap throttling, queue preload, each review decision, optimistic stale rejection, and concurrent transition serialization. Canonical `direnv exec . mix check --verbose` passed 356 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean. Final multi-agent review had no blocker or warning. Browser desktop 1008px and mobile 390px checks covered unauthenticated redirect, sign-in, all three evidence queues, alias mutation, sign-out/re-access rejection, no horizontal overflow, all scoped controls at least 44px, and no console warnings/errors; temporary records/tokens and screenshots were deleted and database absence verified. Next: obtain written source permission and curate real official Polish-English SKU/alias/MSRP evidence through this desk; public Sealed, AshBackpex operations, budgets/source health, aggregates, and buying intelligence remain incomplete.

## [2026-08-09] fixture-backed sealed retailer refresh | Disabled production boundary
- Task completed: Added the source-keyed `SealedRetailerAdapter` callback/strict JSON-safe value contract, one time-specific LootQuest WooCommerce Store API fixture adapter, `SealedRetailerRefresh`, explicit adapter registry/acquisition, and a unique `sealed_retailers` Oban worker/queue. The adapter uses a fixed HTTPS host/path and allowlisted fields/options, bounded pagination/rows/body/request/job execution, Decimal PLN minor-unit conversion, strict direct URLs, source-order/payload retention, entity decoding, and explicit sealed-category plus import/preorder/backorder filtering. Req-level retries are disabled; Oban owns bounded whole-batch transport/rate-limit/server/page-drift/database retries and honors bounded Retry-After values.
- Correctness and boundary: Every fetched row and duplicate source ID is validated before writes. Persistence locks and revalidates the canonical active retailer after fetch, so a concurrent disable prevents later ingest; one transaction covers the full batch, notifications dispatch only after commit, later-row failure rolls back earlier rows, and absent rows remain unchanged instead of becoming fabricated sold-out transitions. Unexpected adapter callbacks and malformed/configuration failures cancel permanently. The runtime adapter registry is empty and no sealed Cron, production retailer/SKU/listing/observation, permission claim, or public offer was added. The immutable LootQuest capture records that public fetchability is not recurring collection/republication permission.
- Files changed: `config/config.exs`; `lib/tcg_cheap/catalogue/sealed_retailer_{adapter,acquisition,refresh,worker}.ex`; `lib/tcg_cheap/catalogue/sealed_retailers/loot_quest.ex`; focused adapter/listing/refresh/worker tests and `test/fixtures/loot_quest/products.json`; raw evidence `knowledge-base/raw/2026-08-09-lootquest-store-api.md`; current architecture/provider/north-star/index articles; and this log.
- Validation: Focused adapter/refresh/listing/worker coverage passed 42 tests, including bounded acquisition, malformed/redirect/rate-limit behavior, transient pagination drift, all-before-write validation, atomic rollback, missing-row preservation, disable-during-fetch rejection, changed-observation behavior, unique enqueue, real Oban completion, and post-completion uniqueness release. `direnv exec . mix check --no-test --verbose` passed while iterating. Canonical `direnv exec . mix check --verbose` passed 337 tests with format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, and Dialyzer clean. `git diff --check` passed. Next production blockers are written source permission and real official Polish-English SKU/alias/MSRP curation; authenticated admin review is the enabling batch while access remains blocked.

## [2026-08-08] sealed listing observations | Immutable transition foundation
- Task completed: Reconciled the preceding committed retailer/listing/mapping foundation and added immutable `SealedListingObservation` persistence to every successful `RetailerListing.ingest`. Observations retain trimmed plus normalized source title, direct HTTPS URL, valid GTIN when present, optional finite positive PLN price, explicit stock state, provider check time, and private source payload. The current projection remains locally auditable through first/last-seen and last-checked timestamps.
- Correctness: A stable per-retailer/source-listing transaction advisory lock closes the concurrent first-upsert race and remains held through observation creation. Stale timestamps and disabled rows cannot be overwritten. Unchanged later checks, including numerically equal Decimal scales, do not duplicate history; changed identity/source/price/stock content and transitions back append. One observation per listing/check timestamp rejects ambiguous contradictions and rolls back the projection; restrictive foreign keys prevent hard listing deletion from cascading retained observations. No live source, retailer-specific HTTP, real retailer/SKU/listing/observation, sold-out inference, public Sealed UI, admin UI, or fabricated history was added.
- Files changed: production `lib/tcg_cheap/catalogue/retailer_listing.ex`, `lib/tcg_cheap/catalogue/changes/{lock_retailer_listing_ingest,record_sealed_listing_observation}.ex`, `lib/tcg_cheap/pricing/sealed_listing_observation.ex`, `lib/tcg_cheap/pricing/changes/normalize_sealed_listing_observation.ex`, `lib/tcg_cheap/pricing/validations/sealed_listing_observation.ex`, and `lib/tcg_cheap/core.ex`; generated migration/snapshot `20260808213452_sealed_listing_observations`; focused observation and independent-transaction concurrency tests; current north-star/architecture/provider/index documentation and this log.
- Validation: 34 focused observation/retailer/listing/mapping/concurrency tests passed, including two first-attempt ingests in independent PostgreSQL transactions. Canonical `direnv exec . mix check --verbose` passed 312 tests with Ash codegen, Sobelow, format, compile, unused dependencies, xref, Credo, and Dialyzer clean. Final code review found no blocker or warning. `mix ash.codegen --check`, deterministic wiki metadata/index/internal+raw link lint, and `git diff --check` passed. Next: obtain approved sealed source access, curate real official Polish-English SKUs/aliases/MSRP evidence, and implement one fixture-backed retailer adapter/refresh path before public offers or buying intelligence.

## [2026-08-08] sealed catalogue foundation | Phase 4 begun, not complete
- Task completed: Documented the source-neutral AshPostgres `SealedProduct` canonical official Polish-English SKU foundation and `SealedProductAlias` name/EAN review foundation. Products support stable slugs/search text, allowlisted types, series/set/release, optional finite positive PLN MSRP with paired provenance/source URL, image, PL/en/official flags, draft/approved/archived and current/discontinued states, source identity/provenance/private payload/timestamps, draft-only stable source+source_id imports, manual curation without source, and locked/revalidated approval. Aliases support normalized values, original values/provenance, pending/approved/rejected queues, approved-per-product reads, GTIN-8/12/13/14 ASCII normalization, GS1 checksum enforcement, and global canonical-product uniqueness. Reviewed rows cannot be overwritten by imports.
- Boundary: Generated migration/snapshot `20260808160842_sealed_catalogue_foundation` was applied and reviewed without hand edits. DB constraints/indexes cover state/timestamp invariants, approved completeness, PL/en, source pairs, finite MSRP, canonical slug/search, type, GTIN/checksum, slug/source identity/product FK/global EAN. No production SKUs, approved source access, retailer adapters, listings, observations, admin UI, or invented prices were added. Public Sealed remains unavailable; Phase 4 steps 1–2 are materially founded but not complete. Next batch is retailer/listing/mapping/observation foundation plus approved source access and real SKU curation.
- Validation: 20 focused sealed tests passed, including direct SQL constraints and independent-transaction expected-error tests. `direnv exec . mix check --verbose` passed 278 tests with Ash codegen/Sobelow/format/compile/xref/unused/Credo/Dialyzer clean; `mix ash.codegen --check` and `git diff --check` passed. A temporary runtime import probe was deleted and verified zero; no production seed data remains. No raw capture was created.

## [2026-08-08] Phase 3 PLN/share batch | Complete
- Task completed: Documented Phase 3 completion: TradeLive reads the latest cached NBP rate locally, subscribes before enqueueing once per connected LiveView, consumes PubSub completion/failure, retains cached conversion while pending/failed, and rejects malformed/noncanonical/nonfinite/future/older rate messages. Unit/row prices remain EUR; side totals and complete differences show Decimal PLN with exact dated NBP evidence, while incomplete values remain explicit.
- Share completed: `TradeShare` copies an absolute server-derived canonical `Composition.to_path` URL containing only stable IDs/quantities. It excludes names, prices, internal UUIDs, pick/unknown parameters, fragments, and noncanonical ordering. Clipboard API and cleaned legacy fallback restore focus, protect overlapping/destroyed instances, expose one accessible feedback live region, and ignore stale/malformed async results. Sealed remains unavailable; no auth, server trade records, frozen snapshots, or provider HTTP exist in public paths. Phase 4 sealed catalogue/acquisition is next; the overall MVP is not complete.
- Validation: Focused TradeLive coverage passed with 26 tests; assets build passed; `direnv exec . mix check --verbose` passed 258 tests with Ash codegen, Sobelow, format, compile, unused, xref, Credo, and Dialyzer clean (0 Dialyzer, no Credo). Detector output was limited to two known explicit square 3px border false positives plus legacy/design-system advisories; evidence typography is aligned to `.75rem`. Browser desktop 1008px/mobile 390px checks passed long-name/large-total, exact rate/PLN, 44px control, no-overflow, Clipboard API/fallback, focus cleanup, failure feedback, and no-console checks. Temporary browser records/rates/jobs were deleted and verified zero; final code review was clean. Deterministic wiki metadata/index/internal+raw link checks and `git diff --check` passed.

## [2026-08-08] NBP EUR/PLN backend | Implemented, public display remains
- Task completed: Documented the committed-next NBP backend batch: AshPostgres `ExchangeRate` canonical Table A EUR/PLN observations by effective date, positive Decimal rate, publication number, fetched timestamp, same-date refresh upsert, retained history, generated migration/snapshot application and review; `ExchangeRateProvider.Result`; strict `NbpExchangeRate` Req adapter using the official HTTPS endpoint with Decimal JSON, future/shape validation, and explicit error mapping; and `ExchangeRateWorker`/`ExchangeRateAcquisition` with queue concurrency 1, active-state uniqueness, retry/cancel/PubSub, subscribe-before-enqueue, once-per-UTC-day freshness, weekend/holiday carry-forward, and static `0 15 * * *` UTC Cron.
- Evidence: Added immutable [2026-08-08 NBP API EUR rate](../raw/2026-08-08-nbp-api-eur-rate.md) capture with official documentation URLs, HTTPS requirement from 2025-08-01, current endpoint, 404/93-day behavior, no published numeric quota found, and time-specific live observation `4.3010`, effective date `2026-08-07`, publication `152/A/NBP/2026`. The live official adapter smoke succeeded. No HTTP runs in LiveView/request paths.
- Boundary/remaining: NBP backend/local cache now exists, but public PLN totals/difference display and explicit share/copy controls remain the two Phase 3 blockers. Phase 3 is not complete; no UI/browser validation is claimed beyond the live provider smoke.
- Validation: NBP-focused tests passed (27); canonical `direnv exec . mix check --verbose` passed with 249 tests; Ash codegen, Sobelow, format, compile, xref, unused-dependency, Credo, and Dialyzer checks passed; generated codegen check was clean; final backend code review was clean with no findings. Deterministic wiki metadata/index/internal+raw link checks and `git diff --check` passed.

## [2026-08-08] Phase 3 trade foundation | Implemented, not complete
- Task completed: Documented the public `/trade` Singles foundation: deterministic bounded URL-only IDs/quantities, safe parser/duplicate merge/canonical sorting, `handle_params` restoration, `push_patch` mutations, one local search with explicit side actions, merged quantities, and untrusted-event protection. Added bulk local card/set/current `tcgdex_cardmarket_v1` reads, pure Decimal EUR row/side/difference valuation, stale/unpriced/incomplete evidence, bounded subscribe-before-request acquisition, PubSub bulk recalculation, stale failure fallback, and shared external autocomplete coverage.
- Interaction completed: Documented side-neutral CardDetail pending pick and strict local return reconstruction, exact identity/relative freshness/acquisition statuses, symmetric desktop ledgers, stacked mobile layout, 44px controls, and 390px overflow protection. Updated Home/design language from colocated to shared external hook and added the Trade surface brief.
- Boundary/remaining: Phase 3 steps 1–3 plus EUR/stale/unvalued/incomplete/responsive foundations are complete. NBP EUR/PLN rate/resource/acquisition/display and explicit share/copy controls remain blockers; the deterministic URL is manually copyable only. Sealed remains unavailable; no auth/server trade records/snapshots/provider HTTP in public paths.
- Validation: canonical `direnv exec . mix check --verbose` passed with 222 tests, 0 Dialyzer errors, no Credo issues, and Ash codegen, Sobelow, format, compile, xref, unused-dependency, assets, and static checks passing; acquisition regression tests are included. Focused acquisition/trade/valuation/composition/bulk/Home/CardDetail coverage passed with 76 tests, and all web tests passed with 46 tests. Intended-diff checks and browser checks passed, covering focus/node/caret/composition, canonical URL operations, CardDetail pick/return, 390px layout, 44px controls, stale failure fallback, and no console errors. Detector retained two known square-border false positives plus advisories. Final read-only code review was clean to commit with no findings after deterministic search-error/live-region component coverage was added.

## [2026-08-08] autocomplete correction | Complete focus-preserving combobox/listbox
- Task completed: Replaced the proven form-level `phx-change`/form-reassignment focus-loss path with a colocated 250ms input hook. The focused input is never server-value patched; query, input node, focus, caret/selection, and mid-string insertion survive result updates. IME behavior is documented only as a browser composition-event simulation: search is blocked during composition and runs once after compositionend. Escape cancels pending debounce.
- Interaction completed: Stable `card-option-UUID` stream IDs and bounded max-10 option records support reinserting adjacent stream items when the active assign changes, with exactly one active/`aria-selected` option. Combobox/listbox semantics include `aria-expanded`, `aria-controls`, `aria-activedescendant`, direct streamed `role=option` children, visible first/active state, wrapping ArrowUp/ArrowDown, exact active Enter selection, Escape close retaining query/focus, validated option click/touch navigation, no nested interactive control, accessible names tied to visible name/set/rarity/price/update IDs, and query-specific live status even for unchanged counts. The active option is visibly ruled; the listbox is bounded to `min(45svh, 32rem)` and scrolls internally while the hook keeps it in view without moving page/input scroll.
- Files changed — production: `lib/tcg_cheap_web/live/home_live.ex`, `test/tcg_cheap_web/live/home_live_test.exs`, `assets/css/app.css`; documentation: `PRODUCT.md`, `DESIGN.md`, `.impeccable/design.json`, `.impeccable/surfaces/lib-tcg-cheap-web-live-home-live-ex.md`, `knowledge-base/wiki/product/mvp-implementation-plan.md`, `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, and this log. No raw files changed.
- Validation: Pre-fix real browser evidence showed focus moved to BODY after the first character and later letters were lost. Canonical `mix check --verbose` passed with 185 tests (Home 15; all web 29; assets/static passed); final code review was clean; `git diff --check`, JSON lint, and deterministic wiki lint passed. Post-fix desktop typed `Scrollcheck` one character at a time with 400ms/server update after each, preserving every prefix, the same node, focus, caret, and mid-string insertion. Mobile preserved focus/caret without horizontal overflow. Ten-option Arrow navigation kept exactly one selection, input focused/visible, internal listbox scroll, and page scrollY 0; Enter navigated the exact active ID; Escape canceled pending debounce, retained `ScrollcheckX`/focus, and stayed closed after 650ms. No console errors. Detector findings were only two known explicit-square 3px border false positives plus advisories. Composition validation was browser event simulation only, not a native IME claim.
- Boundary/next: No provider request, trade capability, or sealed capability was added. The autocomplete blocker is complete/current; Phase 3 URL-backed trade foundations are next. The prior blocker note remains below as pre-fix history, and the owner report/baseline are preserved as historical evidence.

## [2026-08-08] product-owner autocomplete quality correction | Record blocker before implementation
- Task: Docs-only record of the owner’s newest public UX/correctness correction after commit `99023f3`. The owner reports that the current autocomplete “sucks” and loses input focus on each letter. This is now the highest-priority active blocker before Phase 3 or later roadmap features.
- Expected behavior: Preserve the minimal plain collector Home baseline, approved colors/fonts and warm square direction, exact-printing identity, pricing honesty, and clear CTA. The next implementation must preserve query, caret/selection, focus, and IME/composition across LiveView updates without input replacement or focus theft; update results without moving focus; provide combobox/listbox semantics, stable IDs, ArrowUp/ArrowDown active navigation, visible active option, Enter exact-printing selection to price detail, Escape close, touch/click selection, and screen-reader status. Provider calls remain outside request paths.
- Validation required: LiveView regression tests where possible plus real browser typing one character at a time with focus checks after every update, keyboard selection/Escape, mobile and desktop coverage, and no console errors or horizontal overflow. No implementation is claimed in this entry.
- Files changed — documentation only: `knowledge-base/wiki/product/mvp-implementation-plan.md`, `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, this log, `PRODUCT.md`, and `.impeccable/surfaces/lib-tcg-cheap-web-live-home-live-ex.md`. No code, tests, DESIGN, sidecar, or raw files changed.
- Validation: Deterministic wiki metadata/index/link checks and `git diff --check` only.

## [2026-08-08] homepage simplification | Complete minimal collector Home batch
- Task completed: Implemented and documented the minimal Home correction for ordinary collectors. The approved warm square palette/fonts remain; Home now uses a compact wordmark with `Compare Pokémon prices`, Singles/Sealed switch, direct `Find a card` search, no idle copy, short plain states, one-column exact-printing price rows with image/name/set/collector/optional rarity, `€…` or `Price unavailable`, `Updated …` plus `May be outdated`, one solid `View price` CTA, terse shipping language, and collapsed `How prices work` policy/methodology/non-affiliation caveats. Rows avoid visible TCGdex/legality/policy/freshness/local-data jargon. Sealed remains honestly unavailable; no trade feature was added. Exact identity, accessibility, and local-only stream behavior remain intact; `archive-world` remains scoped to card detail.
- Files changed — production Home/root/CSS: `lib/tcg_cheap_web/live/home_live.ex`, `lib/tcg_cheap_web/components/layouts/root.html.heex`, `assets/css/app.css`; Home tests: `test/tcg_cheap_web/live/home_live_test.exs`; surface brief: `.impeccable/surfaces/lib-tcg-cheap-web-live-home-live-ex.md`; product/design/sidecar: `PRODUCT.md`, `DESIGN.md`, `.impeccable/design.json`; wiki: `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, and this log.
- Validation: canonical `mix check --verbose` passed with 180 tests; Home tests 10 and all web tests 24; assets/static checks passed; browser checks at 1440x1000 and 390x844 showed results in the viewport, no horizontal overflow or console errors, >=44px controls, and collapsed details. Final code review was clean; detector findings were only the two known explicit-square 3px border false positives plus design-system advisories; `git diff --check` passed. JSON validation and deterministic wiki metadata/index/link checks passed.
- Boundary/remaining: Sealed and trade capability remain unfinished, with broader roadmap work continuing.

## [2026-08-08] product-owner course correction | Record minimal collector Home direction
- Task: Docs-only record of the newest product-owner correction. Commit `3a55229` is the implementation baseline being corrected: the just-built Home is a functional foundation but remains too wordy and technical for simple collector tasks. The product has exactly two top-level customer features—sealed product price comparison and singles price comparison, with trade composition/difference calculation inside Singles.
- Direction: Simplify Home toward plain language, minimal copy, clear CTAs, and honest simple states while retaining exact-printing correctness, accessibility, legal/methodology caveats, and secondary technical disclosure where feasible. Current colors, fonts, and warm square visual direction stay approved; do not add dead controls or imply unavailable sealed/trade capability. The next implementation batch should simplify Home while Phase 3 trade foundations remain high-leverage.
- Files changed — documentation only: `knowledge-base/wiki/product/mvp-implementation-plan.md`, `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, this log, `PRODUCT.md`, and `.impeccable/surfaces/lib-tcg-cheap-web-live-home-live-ex.md`. No code, tests, DESIGN, sidecar, or raw files changed.
- Validation: Deterministic wiki metadata/index/link checks and `git diff --check` only. No implementation or broader validation is claimed.

## [2026-08-08] homepage decision surface | Complete implementation batch
- Task completed: Documented the built Home decision-world: Singles default, accessible Singles/Sealed-products switch with an honest sealed-unavailable state, direct local exact-printing search, TCGdex image/identity/discriminators, active `tcgdex_cardmarket_v1` valuation loaded with search in one relationship load, current/fresh/stale/unpriced estimate states, visible methodology/disclaimer, and one value-details action. Preserved `archive-world` for card detail.
- Files changed — production/domain: `lib/tcg_cheap/catalogue/card_printing.ex`, `lib/tcg_cheap/catalogue/preparations/search.ex`; web/root/CSS: `lib/tcg_cheap_web/live/home_live.ex`, `lib/tcg_cheap_web/components/layouts/root.html.heex`, `assets/css/app.css`; tests: `test/tcg_cheap/catalogue/search_test.exs`, `test/tcg_cheap_web/live/home_live_test.exs`; surface brief: `.impeccable/surfaces/lib-tcg-cheap-web-live-home-live-ex.md`; product/design: `PRODUCT.md`, `DESIGN.md`, `.impeccable/design.json`; wiki: `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, and this log. No migration or raw capture.
- Validation: canonical `mix check --verbose` passed with 180 tests; focused 25 tests, assets build, and desktop/mobile browser checks at 1440x1000 and 390x844 also passed, with first results in the desktop viewport, no horizontal overflow, and no console errors. Final code re-review found no blocking or warning findings, and `git diff --check` passed. Detector findings remain two known false positives for explicit square 3px input borders plus design-system advisories; these remain advisory and are not claimed to have disappeared.
- Boundary/remaining: No provider request, trade capability, or sealed capability was added. Trade, sealed, admin, budget/operational, and remaining broader product work continue; Home is not claimed final beyond this batch. The preceding benchmark-only entry is preserved below as pre-implementation evidence.

## [2026-08-08] homepage decision-surface benchmark | Record before redesign
- Benchmark: Official [Pokémon TCG Live variant flow](https://support.pokemon.com/hc/en-us/articles/45762566240788-How-to-View-Card-Variants-in-Pok%C3%A9mon-TCG-Live) searches/selects first, then exposes the exact variant/rarity; its [exchange flow](https://support.pokemon.com/hc/en-us/articles/6488739095700-How-Do-I-Exchange-Trade-Credits-to-Obtain-Cards) uses filters to locate the card, with the action happening from the selected card.
- Benchmark: The official [MTG Arena mobile deck-building reference](https://magic.wizards.com/en/news/mtg-arena/mtg-arena-state-game-january-2021-01-21) keeps desktop search/advanced filtering available while mobile uses touch-sized direct add/remove controls.
- Product decision: The first viewport should prioritize mode plus large direct search, image-backed exact identity, the current local estimate or an honest unpriced state, and a clear detail action. Preserve touch targets and keyboard semantics. Sealed mode may be visible only with a clearly unavailable honest state until sealed data exists; avoid archive/catalogue thesis copy.
- This entry is benchmark-only; no implementation or validation is claimed yet.

## [2026-08-07] product course correction and TCGdex imagery | Record owner decisions
- Decisions: The product owner approved TCGdex imagery for the MVP, accepting the pragmatic risk because TCG Cheap is clearly unofficial/non-affiliated and positioned as a review/comparison/decision-support site. This is a product-risk decision, not a legal conclusion or proof that underlying artwork is independently licensed; third-party artwork rights remain an evidence caveat and broader reuse/self-hosting is not approved. The owner also made homepage course correction the highest-priority active public-UI goal: move from the archive/catalogue framing to decision support for singles estimates, fair trades, and Polish sealed-buy decisions. Singles remains the default, Sealed-products mode and direct search/select are authoritative journeys, and unsupported sealed/trade states must remain honest while built.
- Code: commit `52a9c0d` and the `CardImage` boundary hotlink only canonical `https://assets.tcgdex.net` URLs, with high WebP on detail, low WebP in search, no-referrer, exact CSP host, no proxy/cache, and honest missing/invalid fallback. The visible disclaimer names Pokémon, Nintendo, TCGdex, Cardmarket, and listed companies.
- Files changed: `knowledge-base/wiki/product/mvp-implementation-plan.md`, `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, and this log. No raw capture was created.
- Validation: CardImage + web-focused tests 23; canonical `mix check --verbose` passed with 177 tests; assets/check static passed; product owner confirmed valid live image rendering; `git diff --check` passed. Deterministic wiki lint: 4 articles indexed, 12/12 metadata, complete index coverage, relative/Raw links resolve, and no duplicate current entry.
- Remaining/next: Subsequent roadmap agents must pick up the homepage course correction before treating the current Home surface as finished. Prioritize search/results and useful actions in the first viewport, preserving robust local search, exact identity, accessibility, and archive materials only where they serve the task. Phase 3 URL-backed trade foundations may be delivered together with or as enabling work for this correction. Earlier image-withheld/current-Home-complete wording is superseded for current planning; older logs remain historical.

## [2026-08-07] card detail valuation surface | Complete local-first detail/history batch
- Benchmark links and decisions preserved: the [Pokémon TCG Card Database](https://www.pokemon.com/us/pokemon-tcg/pokemon-cards) supports structured identity metadata; [Pokémon TCG Live](https://support.pokemon.com/hc/en-us/articles/45762566240788-How-to-View-Card-Variants-in-Pok%C3%A9mon-TCG-Live) provides contextual variants but is not a physical-market pricing reference; [Arena reprints](https://magic.wizards.com/en/news/mtg-arena/upcoming-improvements-to-reprints) distinguishes printings/styles/quantity; and the [Arena mobile deck builder](https://magic.wizards.com/en/news/mtg-arena/state-game-adventures-forgotten-realms-2021-07-02) favors larger mobile presentation and tap actions. Retained archive-wall grammar, identity/current value first, fixed 30-day daily history with accessible ledger, and no range tabs, seller-count/condition/language claims, swipe navigation, or external images.
- Task behavior: completed public `/cards/:tcgdex_id` exact-printing detail linked from local search; local/disconnected valuation and bounded history reads; subscribe-before-request freshness-gated Oban acquisition; card-specific PubSub completion/failure reconciliation; independent current-value preservation on history failure; explicit fresh/stale/fetching/unpriced/read-error/terminal-failure and insufficient-history collecting states; Decimal SVG plus accessible ledger; source/metric/policy/timestamp/methodology/disclaimer; mobile identity/value first; no image pending rights; no trade CTA before Phase 3.
- Files changed — production/domain: `lib/tcg_cheap/core.ex`, `lib/tcg_cheap/pricing/singles/single_valuation_snapshot.ex`, `lib/tcg_cheap/pricing/singles/valuation_history.ex`; web: `lib/tcg_cheap_web/router.ex`, `lib/tcg_cheap_web/live/home_live.ex`, `lib/tcg_cheap_web/live/card_detail_live.ex`; CSS: `assets/css/app.css`; tests: `test/tcg_cheap/pricing/singles/valuation_history_test.exs`, `test/tcg_cheap/pricing/singles/valuation_history_since_test.exs`, `test/tcg_cheap_web/live/card_detail_live_test.exs`, `test/tcg_cheap_web/live/home_live_test.exs`; surface brief: `.impeccable/surfaces/lib-tcg-cheap-web-live-card-detail-live-ex.md`; wiki: `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, `knowledge-base/wiki/log.md`. No raw capture.
- Validation: focused history+web tests 19; canonical `mix check --verbose` passed 168; `mix check --no-test` passed after final fixes; assets build passed; 1440x1000 and 390x844 browser checks passed with value in the first mobile viewport, no horizontal overflow, and no console errors. Mechanical detector had one known false-positive warning on the existing square 3px search-input bottom border and advisories; new off-palette hover/type drift corrected; code-review blockers/warnings fixed; `git diff --check` clean. Deterministic read-only wiki lint: exactly 4 articles indexed, 12/12 metadata fields valid, all indexed paths/articles covered, all relative/Raw links resolve, no duplicate index/log entry.
- Remaining/next: external card imagery remains withheld pending rights; trade waits on Phase 3; history is honest/collecting until real observations accrue; UTC daily grouping/time labels with Europe/Warsaw presentation are deferred. Real full-catalogue validation, operations/budgets/admin/scheduling, and sealed work remain. Next coherent high-leverage playability batch is Phase 3 URL-backed trade composition/search/add-side/quantity foundations, enabling the card-detail trade action. Earlier operations/admin/budget and sealed-source roadmap work remains unfinished, so this does not declare prior phases globally complete.

## [2026-08-07] Oban valuation acquisition | Complete freshness-gated jobs and PubSub boundary
- Task completed: Recorded Oban 2.23 migration/configuration with a valuations queue at concurrency 4 and a seven-day Pruner; `ValuationAcquisition` freshness-gated, deduplicated exact-printing jobs; server-side TCGdex adapter configuration; and a worker that validates identity, policy, EUR value, and provenance, fetches outside transactions, records through the existing snapshot action with active uniqueness, classifies retryable/permanent failures, and emits card-specific success and terminal-failure PubSub events. Recorded the subscribe-before-request helper and the invariant that no provider runs on the render path.
- Files changed — wiki only: `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, and this log. No raw capture was created; no code or git metadata was changed.
- Validation: valuation worker 14 tests; singles pricing 56 tests; canonical `mix check --verbose` passed with 155 tests; final review clean; deterministic wiki lint passed with 4 articles, 12/12 metadata fields, 4/4 index coverage, 0 issues, and 0 auto-fixes; `git diff --check` clean.
- Remaining/next: Card detail LiveView consumption of freshness/job/PubSub state and the 30-day history graph; production operational tracking/budgets; full scheduling; and other card detail/trade/sealed/admin surfaces. The valuation job and PubSub boundary are complete, but UI consumption is not claimed.

## [2026-08-07] detailed set enrichment and importer concurrency | Complete catalogue enrichment batch
- Task completed: Recorded `TcgCheap.Catalogue.Enrichment.enrich_set/2` as the detailed set-level per-card enrichment/Cardmarket mapping service: one timed detailed-set fetch; exact identity, TCG Pocket, canonical-list, duplicate/invalid-ID, truncation, and 1,000-card fan-out guards before card calls/writes; at-most-once ordered bounded card fetches; isolated tagged callback failures/timeouts; one post-fetch clock and shared normalized UTC microsecond timestamp; numeric aggregate outcomes and ordered stage-tagged failures. Recorded `import_fetched_card/4`, provider-return normalization, explicit expected IDs, stale/idempotent preservation, cross-set rejection, set-before-card advisory lock ordering, and preservation of existing `CardSet` metadata.
- Files changed — production: `lib/tcg_cheap/catalogue/enrichment.ex`, `lib/tcg_cheap/catalogue/importer.ex`; tests: `test/tcg_cheap/catalogue/enrichment_test.exs`, `test/tcg_cheap/catalogue/importer_test.exs`, `test/tcg_cheap/catalogue/sync_test.exs`; wiki: `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, and this log. No raw capture was created.
- Validation: Focused Importer/Sync/Enrichment tests 56; all catalogue tests 79; real concurrency line 20/20; canonical `mix check --verbose` passed 141 tests including format, Ash codegen, Sobelow, warnings-as-errors compile, unused dependencies, xref, Credo, Dialyzer, and tests; `git diff --check` clean.
- Limitations/next actions: This remains a callable background-domain service, not an Oban job or public request path; no external provider call was added to public paths. Production full-catalogue scheduling, acquisition-run/budget/source-health tracking, valuation refresh/Oban/PubSub, and other product surfaces remain unfinished. The unresolved live TCGdex set-list timeout remains. Next coherent Phase 2 task: wire `tcgdex_cardmarket_v1` valuation acquisition/persistence through a production-safe deduplicated Oban path and PubSub.
- Deterministic wiki lint: 4 articles exist and are indexed; 12/12 metadata fields valid; 4/4 index coverage; all relative and Raw links resolve; 0 issues and 0 auto-fixes.

## [2026-08-07] public exact-printing search and concurrency hardening | Complete search surface batch
- Task completed: Recorded the public `/` `HomeLive` local-only exact-printing search surface with `to_form`, 250ms `phx-change` debounce, shared Unicode normalization, effective minimum 2/max 100 handling, stream-reset results, stable accessible state IDs/`aria-live`, distinct same-name printings, backend-loaded `CardSet`, and no provider HTTP, invented price, or image in render/event paths. Recorded the responsive scoped archive-wall design, self-hosted Barlow Condensed/Azeret Mono fonts with OFL notices, accessible responsive labels/focus/reduced-motion behavior, and neutral shared Layout boundary. Hardened Catalogue Sync to use the full importer's per-card transaction advisory lock in sorted brief-ID order, closing the brief/full unique-ID race and lock-order cycles; provider fetches remain outside transactions.
- Files changed — production: `lib/tcg_cheap/catalogue/sync.ex`, `lib/tcg_cheap_web/live/home_live.ex`, `lib/tcg_cheap_web/components/layouts.ex`, `lib/tcg_cheap_web/components/layouts/root.html.heex`; visual/assets: `assets/css/app.css`, `PRODUCT.md`, `DESIGN.md`, `.impeccable/design.json`, `priv/static/fonts/`; tests: `test/tcg_cheap_web/live/home_live_test.exs`; wiki: `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, and this log. Router, importer/search modules, and catalogue/concurrency tests were not changed in this batch, though they were executed. No raw capture was created.
- Validation: HomeLive 6 tests passed; all web tests 10 passed; catalogue tests 56 passed; focused concurrency regression passed 20/20 repetitions. Canonical `mix check --verbose` passed with 118 tests. Assets build, codegen/static checks, migrations, JSON/design docs, `git diff --check`, desktop/mobile browser checks, keyboard focus, no-overflow/no-console checks, and 200% text reflow passed. Impeccable reported one false positive for a 3px input bottom border with computed radius zero. Final deterministic wiki lint: 4 articles, 12/12 metadata fields, 4/4 index coverage, 16 relative links, 4 raw links, zero issues.
- Limitations/next actions: No card alias model, detailed enrichment/Cardmarket mapping, valuation refresh/Oban/PubSub, card detail/trade/sealed/admin surfaces are complete; ranking still needs real full-catalogue tuning and the TCGdex live list timeout remains. Image licensing remains unresolved. Next coherent Phase 2 task is detailed enrichment/Cardmarket mapping or the valuation job path afterward, not another search foundation task. Broader product UI remains incomplete.

## [2026-08-07] exact-printing search | Complete local PostgreSQL search batch
- Task completed: Recorded `CardPrinting.search` exposed as `TcgCheap.Core.search_card_printings`. Search is local PostgreSQL only with no provider/HTTP calls. Private persisted fields derive from name, set, collector number, and TCGdex ID on every create/upsert using shared Unicode NFKC, Unicode whitespace collapse, trim, and Unicode lowercase normalization; SQL backfill parity uses PostgreSQL 18 `pg_unicode_fast`. `pg_trgm` is installed and four concurrently-created GIN trigram indexes support escaped case-insensitive contains and indexed `%` candidates. Deterministic bounded ranking covers exact IDs/names/collectors/sets, prefixes, trigram similarities, Standard legality tie-breaking, and stable identity fields; defaults are 10, hard max 20, minimum query length 2. Mapping statuses and old/non-Standard cards remain searchable, exact printings are not collapsed, and `CardSet` is loaded. No public UI is claimed.
- Files changed — production: `lib/tcg_cheap/catalogue/card_printing.ex`, `lib/tcg_cheap/catalogue/search_text.ex`, `lib/tcg_cheap/catalogue/changes/set_search_text.ex`, `lib/tcg_cheap/catalogue/preparations/search.ex`, `lib/tcg_cheap/core.ex`, `lib/tcg_cheap/repo.ex`; generated: `priv/repo/migrations/20260807162813_add_card_printing_search_extensions_1.exs`, `priv/repo/migrations/20260807171515_add_normalized_card_printing_search.exs`, `priv/repo/migrations/20260807171516_add_normalized_card_printing_search_1.exs`, `priv/resource_snapshots/repo/card_printings/20260807171517.json`, `priv/resource_snapshots/repo/extensions.json`; tests: `test/tcg_cheap/catalogue/search_test.exs`; wiki: `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, and this log.
- Validation: Final code review clean. Focused search tests passed (14); all catalogue tests passed (56); canonical `mix check --verbose` passed (113). `mix ash.codegen --check`, migrations, and `git diff --check` passed. Live PostgreSQL `EXPLAIN` with sequential scans disabled showed `BitmapOr` using all four GIN indexes for both `ILIKE` and `%`. SQL/Elixir Unicode parity passed for full-width, decomposed accents, NBSP, U+0130, and contextual Greek sigma.
- Limitations/next actions: No `CardPrinting` alias source/model or public/autocomplete LiveView yet. Ranking needs tuning against a successfully imported real full catalogue. Detailed per-card enrichment, Cardmarket mapping, Oban, and PubSub remain pending; the TCGdex live list timeout limitation remains. Next coherent Phase 2 work is a public exact-printing search/result surface and/or detailed enrichment/mapping.

## [2026-08-07] set enumeration and brief-card sync | Complete TCGdex catalogue sync batch
- Task completed: Recorded strict fixed-endpoint `Provider`/`Tcgdex.list_sets/1`, `Catalogue.Sync.sync_set/2`, and `sync_all_sets/1`. Detailed set payloads are fetched outside writes; `cardCount.total` must equal the card-brief length; every brief is validated before one set transaction; pending minimal card rows are seeded without per-card detail calls; enriched/mapped rows and existing images survive nil brief images through conditional upsert; cross-set card conflicts reject atomically; `serie.id == "tcgp"` is excluded as TCG Pocket; and all-set sync continues other sets while returning ordered aggregate failures/exclusions.
- Files changed: `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, and this log. No raw capture was created.
- Validation: Deterministic adapter/sync coverage passes. The bounded live list smoke remains limited: the first run with default options exhausted the 30-second evaluation, and an explicit retry-disabled attempt returned a `Req.TransportError` timeout; no live list success is claimed. Canonical `mix check --verbose` passed with 99 tests; `git diff --check` passed. Deterministic read-only wiki lint: 4 articles, 12/12 metadata fields, 4/4 index coverage, 16 relative links, 4 raw links, zero issues.
- Remaining work: Detailed per-card enrichment, Cardmarket mapping, automated scheduling/Oban, local exact-printing search, and search/UI remain pending; resolve bounded list-endpoint access/reliability before production scheduling. Image/IP and redistribution licensing and broader sealed research remain open.

## [2026-08-07] catalogue import batch | Add CardSet storage and bounded TCGdex importer
- Task completed: Recorded the new `CardSet` and expanded `CardPrinting` AshPostgres storage, nullable indexed set relationship, metadata/legalities/assets/internal source payload, source/sync and `mapping_updated_at` timestamps, Cardmarket mapping state, and Ash/PostgreSQL mapping/count `CHECK` invariants. Recorded the fixed-endpoint TCGdex provider and one-card-plus-set importer: fetches occur outside transactions; IDs and numeric `localId`/assets are normalized; set/card writes are atomic and idempotent; advisory locking, `FOR UPDATE`, and source/mapping timestamps reject stale response regression. Mapping is conservative and sends ambiguous/material variants to review without guessed IDs. This explicitly does not claim Phase 2 catalogue completion.
- Files changed — production: `lib/tcg_cheap/catalogue/{card_printing.ex,card_set.ex,importer.ex,provider.ex,tcgdex.ex,validations/mapping_invariant.ex}`, `lib/tcg_cheap/core.ex`; generated migration/snapshots: `priv/repo/migrations/20260807150320_add_tcgdex_catalogue_import.exs`, `priv/resource_snapshots/repo/card_printings/20260807150321.json`, `priv/resource_snapshots/repo/card_sets/20260807150322.json`; catalogue fixtures/tests: `test/fixtures/tcgdex/catalogue/`, `test/tcg_cheap/catalogue/`; wiki: `knowledge-base/wiki/{architecture/application-foundation.md,architecture/provider-acquisition-feasibility.md,index.md,log.md}`.
- Validation: Fixture tests covered errors, retries, idempotency, rollback, stale-response rejection, mapping review, assets, IDs, and database constraints. Bounded live adapter smoke succeeded for `swsh3-136` and `swsh3` with default options and no persistence. Canonical `mix check --verbose` passed with 81 tests after resetting only the disposable test DB to clear locally applied superseded uncommitted migration timestamps; `mix ash.codegen --check` and `git diff --check` passed. Read-only deterministic wiki lint: 4 articles, 12/12 metadata fields, 4/4 index coverage, 16/16 relative links, 4/4 Raw links, zero structural issues.
- Remaining work: This imports one explicitly requested card and set only. Full catalogue enumeration/sync, local PostgreSQL exact-printing search, Oban scheduling/valuation acquisition, PubSub, public UI, admin review queue, immutable catalogue-observation history, and production catalogue import remain. The nullable `CardSet` relationship remains for legacy/minimal records; image/IP and redistribution licensing and broader sealed research remain open. Next priority is full set/card sync/enumeration and local search, then Oban/PubSub/UI.

## [2026-08-07] singles snapshot replacement | Serialize current valuation writes
- Task completed: Made `record_single_valuation` transaction-safe by locking the parent `CardPrinting` with `FOR UPDATE`, archiving only the prior current snapshot for the exact card and policy, and inserting the replacement. Policy-specific history is retained and the partial unique current-row index remains defense-in-depth. Automatic external fetch/background/UI integration is not claimed complete.
- Files changed — production: `lib/tcg_cheap/catalogue/card_printing.ex`, `lib/tcg_cheap/core.ex`, `lib/tcg_cheap/pricing/singles/single_valuation_snapshot.ex`, `lib/tcg_cheap/pricing/singles/changes/replace_current_snapshot.ex`; tests: `test/tcg_cheap/pricing/singles/single_valuation_snapshot_test.exs`, `test/tcg_cheap/pricing/singles/single_valuation_snapshot_concurrency_test.exs`; wiki: `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/index.md`, `knowledge-base/wiki/log.md`.
- Validation: `mix compile --warnings-as-errors`; seven focused snapshot tests; 42 tests in the singles pricing directory; `mix ash.codegen --check`; independent dev-runtime smoke with two separate concurrent writes ending in two history rows and one current row, cleaned afterward; final `mix check --verbose` passed with 57 tests; deterministic wiki lint passed with 4 articles, 12/12 metadata fields, 4/4 index coverage, 16 relative links, and zero missing/issues; `git diff --check`.
- Remaining work: Validate catalogue import/mapping and material-variant review, then add Oban acquisition/PubSub and public UI. Keep external provider fetching out of request paths. Broader sealed-source research, destination eligibility, and image/data licensing remain open.

## [2026-08-07] singles valuation storage | Add local snapshot and freshness foundation
- Task completed: Added AshPostgres-backed `CardPrinting` and `SingleValuationSnapshot` resources to `TcgCheap.Core`. Exact printings have a unique TCGdex ID; snapshots retain the active aggregate policy/source/metric, positive Decimal EUR value, fetch and provider-update timestamps, optional Cardmarket product ID, and current/archive status. The generated migration/snapshots add a partial unique index for one current snapshot per card/policy and a history-query index. `Freshness` deterministically classifies missing, fresh, and stale valuations, with the seven-day boundary stale.
- Files changed: `lib/tcg_cheap/core.ex`, `lib/tcg_cheap/catalogue/card_printing.ex`, `lib/tcg_cheap/pricing/singles/single_valuation_snapshot.ex`, `lib/tcg_cheap/pricing/singles/freshness.ex`, generated Ash migration/snapshots, focused tests, and these architecture/log articles.
- Validation: `mix compile --warnings-as-errors`, `mix ash.codegen --name add_card_printing_valuations` (no subsequent drift), six focused persistence/freshness tests, and canonical `mix check --verbose` passed (53 tests). The test setup applied the generated migration against the local test database.
- Remaining notes: This is local persistence only. Implement transaction-safe automatic archival/replacement, catalogue import/mapping review, Oban deduplicated acquisition and PubSub, then public card/trade UI. No provider call runs on a public render path.

## [2026-08-07] aggregate pricing adapter | Implement TCGdex Cardmarket aggregate boundary
- Task completed: Recorded the implemented `lib/tcg_cheap/pricing/singles/tcgdex_cardmarket.ex` background-acquisition adapter. It fetches the fixed TCGdex card endpoint with Req, decodes Decimal values through Jason, selects `avg7` -> `avg30` -> `trend` -> `avg` -> `low`, rounds half-up to two decimals, returns `tcgdex_cardmarket_v1` provenance/timestamps/Cardmarket ID, retries safely within bounded limits, and returns tagged errors. It is not integrated into Ash resources, Oban, storage, or UI. Preserved `Provider`/`Offer`/`Valuation.default_v1` as historical/post-MVP seller-level capability.
- Files changed: `lib/tcg_cheap/pricing/singles/tcgdex_cardmarket.ex`, `test/tcg_cheap/pricing/singles/tcgdex_cardmarket_test.exs`, `test/fixtures/tcgdex/base1-4.json`, and the five wiki files updated in this batch.
- Validation: Fixture-backed deterministic coverage passed with 24 adapter tests and 32 total tests under the singles pricing directory. Default tests use local Req stubs and no live external dependency. Canonical `mix check --verbose` passed with 47 tests, including format, Ash codegen, Sobelow, compile, unused dependencies, xref, Credo, Dialyzer, and tests. Final read-only code review found no remaining blocking- or warning-level code issues after request-option whitelisting, huge-Decimal safe fallback, and status-before-decode fixes. Bounded live runtime smoke validation normalized successful `:avg7` results for canonical TCGdex IDs `base1-4`, `base1-58`, and `sv03-125`; volatile prices were not preserved. Deterministic wiki lint reported 4 articles, 12/12 metadata fields, 4/4 index coverage, 16 relative links, and zero issues.
- Remaining/blocking notes: Validate catalogue exact-printing mappings; add Ash snapshots/storage and seven-day freshness; implement background Oban acquisition and UI integration. Keep seller/offer count unavailable and unfabricated (future shared storage may make it nullable/optional). Broader sealed-source and seller-level research remains open. No new raw capture was needed because the existing provider experiment capture records the TCGdex endpoint/schema evidence.

## [2026-08-07] scrappy singles acquisition spike | Supersede acquisition policy and record provider evidence
- Task completed: Recorded the product-owner supersession of the former strict compliance boundary, bounded credentialed TCG Scraper/cardmarketapi.com experiments, direct Cardmarket 403, Apify/Parse candidate evidence, and the exact provider-neutral valuation boundary. No provider secret was retained; the temporary external-account footprint is documented honestly.
- Files changed: `lib/tcg_cheap/pricing/singles/provider.ex`, `lib/tcg_cheap/pricing/singles/offer.ex`, `lib/tcg_cheap/pricing/singles/valuation.ex`, `test/tcg_cheap/pricing/singles/provider_test.exs`, `test/tcg_cheap/pricing/singles/valuation_test.exs`, `knowledge-base/raw/2026-08-07-scrappy-singles-acquisition-spike.md`, `knowledge-base/wiki/product/mvp-implementation-plan.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/index.md`, and this log.
- Validation: Focused tests passed with 8 tests; deterministic wiki lint checked 4 compiled articles, 12/12 metadata fields, 4/4 index coverage, and 16/16 relative links with zero missing/duplicate issues; `mix check --verbose` passed with 23 tests; `git diff --check` passed.
- Remaining/blocking notes: Obtain a persistent project-owned Apify or Parse credential, run three representative Pokémon products, verify duplicate-seller handling and destination eligibility, then implement only the passing adapter. Configure account pools and global cost reservation later without committing secrets.

## [2026-08-07] phase 0 provider/source research | Record acquisition feasibility batch
- Task completed: Captured bounded read-only real HTTP observations and reconciled them against primary provider, retailer, terms, robots, quota, and pricing sources. Recorded TCGdex/Pokémon TCG API metadata direction, REBEL Hurt sealed discovery direction, and the exact singles acquisition blocker without changing the north star or claiming Phase 0 completion.
- Files changed: `knowledge-base/raw/2026-08-07-provider-source-experiments.md`, `knowledge-base/wiki/architecture/provider-acquisition-feasibility.md`, `knowledge-base/wiki/architecture/reference-project-conventions.md`, `knowledge-base/wiki/index.md`, `knowledge-base/wiki/architecture/application-foundation.md`, and this log.
- Validation: Bounded HTTP experiments/primary-source reconciliation; deterministic wiki lint checked 4 compiled articles, 12/12 metadata fields, 4/4 index coverage, and 14/14 relative links with zero missing/duplicate issues; `mix check --verbose` passed with 15 tests; `git diff --check` passed. Older unresolved wording was reconciled as historical/stale.
- Remaining/blocking notes (historical, superseded by the newer scrappy policy and provider ADR): Obtain written Cardmarket/licensed-provider terms or validate CardTrader with legitimate credentials and bounded exact-offer/Poland-shipping tests; request REBEL and retailer/LGS feed permissions; complete image/data licensing review. No credentials are committed.

## [2026-08-07] phase 0 references | Audit Firmowid and Onside conventions
- Task completed: Audited the two read-only reference repositories against the Phase 0 categories, recorded authority, adopted/rejected conventions, version compatibility, and implications for later phases. Firmowid was clean; Onside already had an unrelated modified `shell.nix`; neither reference was changed.
- Files changed: `knowledge-base/wiki/architecture/reference-project-conventions.md`, `knowledge-base/wiki/index.md`, `knowledge-base/wiki/architecture/application-foundation.md`, and this log.
- Validation: Source/commit/path evidence reconciled; wiki metadata, index coverage, and internal links checked; `mix check --verbose` passed with 15 tests; `git diff --check` passed. Reference commits: `8a18f66aa28ac8444b5b3445fa3c7b6613fdf056` and `ac8d1d942a086bfe868043274fc0f430f30aacef`.
- Historical status at this entry: Provider/source experiments, cost ADR, acquisition feasibility, authentication/admin/background dependency validation, and subsequent MVP implementation phases remain unfinished. The newer provider ADR supersedes this entry's provider-research status; it does not claim the remaining tasks complete.

## [2026-08-07] bootstrap verification | Complete final local validation
- Validation: Full `mix check` passed with 10 tests; homepage returned HTTP 200; LiveView mounted without current console errors; Tidewave MCP ping returned HTTP 200; PostgreSQL loopback connectivity was healthy.
- Deferred/hand-off state: Caddy integration and CI remain explicitly deferred. The app was intentionally left running after verification.

## [2026-08-07] bootstrap hardening | Fix lifecycle, CSP, and quality-gate blockers
- Task completed: Bound Compose PostgreSQL to loopback, made Compose failures actionable, isolated non-main identities with deterministic branch hashes, hardened `dev.up` service/readiness checks and tmux/fallback lifecycle behavior, moved theme initialization into the self-hosted asset, tightened CSP, and made checks non-mutating with Ash codegen validation.
- Files changed: `local/compose.yml`, lifecycle Mix tasks/tests, `assets/js/app.js`, root layout/router CSP, `.gitignore`, `.env.example`, `README.md`, `AGENTS.md`, and this foundation/log documentation.
- Validation: Pending the requested local format, compile, asset, codegen, security, lint, lifecycle, full test, Nix, Compose, Worktrunk, and diff checks.
- Remaining/blocking notes: The product MVP north star remains unchanged; product resources, authentication, jobs, and storage are still future work.

## [2026-08-07] product direction | Designate the MVP implementation plan as the current north star
- Task completed: Marked the product-owner MVP implementation plan as the authoritative current product north star; its requirements, phases, documentation, and acceptance criteria must be completed in full unless explicitly superseded by the product owner.
- Files changed: `knowledge-base/wiki/product/mvp-implementation-plan.md`, `knowledge-base/wiki/index.md`, `knowledge-base/wiki/architecture/application-foundation.md`, `.opencode/skills/llm-wiki/SKILL.md`, `AGENTS.md`, `lib/tcg_cheap_web/endpoint.ex`.
- Validation: Wiki metadata/link checks, formatting, compile with warnings as errors, and `mix check --no-test` completed locally; no CI was added.
- Remaining/blocking notes: Product resources, authentication, background jobs, and application storage remain to be implemented according to the north star.

## [2026-08-07] codebase update | Move MVP specification and bootstrap wiki
- Task attempted: Moved the complete product-owner specification into the product wiki and initialized the TCG Cheap knowledge base.
- Files changed: `knowledge-base/wiki/product/mvp-implementation-plan.md`, `knowledge-base/wiki/architecture/application-foundation.md`, `knowledge-base/wiki/index.md`, `knowledge-base/wiki/log.md`, `knowledge-base/raw/.gitkeep`, `.opencode/skills/llm-wiki/SKILL.md`.
- Validation: Compile, assets setup, and `mix dev.up`/`mix dev.down` are known validation targets; Caddy remains deferred. Wiki content, metadata, index entries, and links were checked locally.
- Remaining/blocking notes: Agent quality tooling is still being established; no product resources, authentication, jobs, or storage exist yet.
