# Application Foundation

- Updated: 2026-08-08
- Sources: Project code; local validation; `PRODUCT.md`; `DESIGN.md`; `.impeccable/design.json`
- Raw: N/A — codebase update

## Current state

TCG Cheap is a Phoenix LiveView application using the `TcgCheap.Core` Ash domain and `TcgCheap.Repo` AshPostgres repository. The local catalogue includes `CardSet` and exact-identity `CardPrinting` storage, with metadata, legalities, assets, source payload, sync timestamps, and conservative Cardmarket mapping state. `CardSet` is nullable for legacy/minimal records. The active TCGdex aggregate is a background-acquisition boundary; it is not called by public rendering or events, and no seller/offer count is fabricated.

The repository is developed in a Nix devenv with Elixir, PostgreSQL, Podman/Compose, Worktrunk, Tailwind, and esbuild tooling. The historical provider-neutral singles core exists in `lib/tcg_cheap/pricing/singles/provider.ex`, `offer.ex`, and `valuation.ex`, with focused tests in `test/tcg_cheap/pricing/singles/provider_test.exs` and `valuation_test.exs`. `Provider`/`Offer`/`Valuation.default_v1` remain historical/post-MVP seller-level capability; `Offer` normalizes seller/language/condition/shipping/Decimal EUR fields and `Valuation` calculates five-lowest-distinct-seller values. The active aggregate adapter uses Req against the fixed TCGdex card endpoint, Jason Decimal decoding, deterministic metric selection, half-up two-decimal rounding, `tcgdex_cardmarket_v1` provenance/timestamps/Cardmarket ID, safe bounded retries, tagged errors, and server-side configuration. Valuation acquisition is integrated through Oban 2.23 with a valuations queue (concurrency 4) and seven-day Pruner; `ValuationAcquisition` creates freshness-gated, deduplicated exact-printing jobs. Its worker validates identity, policy, EUR value, and provenance; fetches outside transactions; records through the existing snapshot action with active uniqueness; classifies retryable versus permanent failures; and broadcasts card-specific success and terminal-failure PubSub events. A subscribe-before-request helper closes the LiveView race. No provider call runs on a public render path.

The active TCGdex aggregate intentionally has no seller/offer count field. The public UI must show source plus selected metric/methodology, not an unknown-count widget; no count may be fabricated, and a future shared storage field may be nullable/optional. Scraper paths remain post-MVP research, with broader seller-level acquisition and destination-eligibility validation still open.

Strict TCGdex set enumeration and brief-card sync fetch provider payloads outside writes, validate counts and briefs before one transaction, preserve enriched rows/images, and seed pending minimal cards. Catalogue Sync uses per-set locking and now acquires the same per-card transaction advisory lock as the full importer, sorting brief card IDs before locking. This closes the intermittent brief/full unique-ID race and prevents lock-order cycles across overlapping sets. Provider fetches remain outside transactions. Cross-set conflicts are rejected atomically; TCG Pocket sets are excluded as designed. The one-card importer also uses `FOR UPDATE` and source/mapping timestamps to reject stale responses. Import and brief sync consistently acquire PostgreSQL advisory locks set-before-card; real concurrent Sync plus fetched Importer coverage passed 20/20 repetitions.

Mapping is conservative: only one explicit Cardmarket ID with no material ambiguity is matched; first-edition, `wPromo`, stamps, pre-release, jumbo, special foil, nonstandard-size or material subtypes, multiple material identities, and multiple Cardmarket IDs are review states with no guessed ID. Finish-only normal/reverse/holo differences may be simplified. `TcgCheap.Catalogue.Enrichment.enrich_set/2` fetches one detailed set, validates exact identity and canonical card lists before writes, then fetches each card at most once with ordered bounded concurrency and imports successful payloads under one shared normalized UTC microsecond timestamp. It rejects malformed/truncated lists, duplicate/invalid IDs, and fan-out above 1,000 before card calls; unexpected callback outcomes and timeouts are isolated so later cards continue. The importer exposes `import_fetched_card/4` with explicit expected IDs and an import/stale outcome while preserving `import_card/2`'s public card return. Cross-set reassignment, including stale payloads, is rejected; existing `CardSet` metadata is preserved during card enrichment, while set sync owns subsequent metadata refresh and can create missing sets. Reruns are idempotent and stale provider card/mapping data is preserved. `SingleValuationSnapshot` retains positive EUR aggregate valuations, policy/source/metric provenance, fetched and provider-update timestamps, optional Cardmarket product ID, and immutable current/archive state. `record_single_valuation` locks the parent `CardPrinting` with `FOR UPDATE`, archives only the prior current snapshot for the exact card and policy, and inserts the replacement. Policy-specific history is preserved, and seven-day freshness classification remains defined.

Local exact-printing search is exposed as `TcgCheap.Core.search_card_printings`. Private persisted fields derive from name, set, collector number, and TCGdex ID through shared Unicode NFKC, whitespace collapse, trim, and lowercase normalization; SQL backfill parity uses PostgreSQL 18 `pg_unicode_fast`. Four concurrent GIN trigram indexes support escaped contains and `%` candidates. Ranking is deterministic and bounded (exact IDs/names/collectors/sets, prefixes, similarities, Standard legality tie-break, stable identity); default 10, hard max 20, effective minimum 2. All mapping statuses and non-Standard cards remain searchable; exact printings are never collapsed and `CardSet` is loaded.

`/` is now a public local-only exact-printing search surface in `HomeLive`. It uses the shared external 250ms `CardAutocomplete` hook used by Home and Trade, backend-shared normalization, effective minimum 2/max 100 handling, and LiveView stream resets. The focused input is never server-value patched: query, node, focus, caret/selection survive result updates; composition pauses search until compositionend and Escape cancels debounce. Idle, short, invalid, empty, error, and results states have stable accessible IDs and `aria-live`; same-count result updates still change the query-specific status. Same-name printings remain separate and show original name/set/collector plus optional rarity; exact TCGdex identity remains in internal routing/data. No provider HTTP runs from render/event paths and no price is invented. Current commit `52a9c0d` renders approved TCGdex imagery only through the strict `CardImage` boundary: canonical `https://assets.tcgdex.net` URLs, low WebP search images, no-referrer, exact CSP host, no proxy/cache, and honest missing/invalid fallback.

Home now uses a scoped `decision-world`: a bright card-shop valuation bench distilled for ordinary collectors with warm bench/paper/ink/orange tokens, square rules, Barlow Condensed action hierarchy, Azeret Mono supporting copy, an accessible Singles/Sealed switch, direct `Find a card` search, and one-column exact-printing price rows. Rows show image, name, set, collector number, optional rarity, `€…` or `Price unavailable`, `Updated …` plus `May be outdated`, and one solid `View price` CTA. Full policy/methodology/non-affiliation caveats are collapsed under `How prices work`; rows avoid TCGdex, legality, policy, freshness, and local-data jargon. Singles is the default; Sealed is an honest unavailable state. The active `tcgdex_cardmarket_v1` valuation is loaded with search in one relationship load, with no N+1 valuation read. The `archive-world` remains scoped to card detail and was not removed globally. Generic shared Layout remains neutral.

The public `/cards/:tcgdex_id` exact-printing detail is now linked from local search. It is local-first: the initial render and events read the local printing and current valuation, while a disconnected/static local valuation and bounded-history read remains useful without a provider. When freshness requires work, the LiveView subscribes before requesting the deduplicated Oban acquisition; card-specific PubSub completion and terminal-failure events reconcile the final state. The current value is preserved independently when the history read fails. The surface explicitly represents fresh, stale, fetching, unpriced, read-error, and terminal-failure states, with an insufficient-history collecting state until real observations accrue.

History is a fixed 30 UTC-calendar-date query/projection: at most the latest fetched/created observation with a stable ID per day, with no future or older points and gaps left disconnected. Decimal values drive the SVG geometry, alongside an accessible ledger. The detail includes source, metric, policy, timestamp, methodology, and a disclaimer; mobile identity and value come first. Approved TCGdex detail imagery uses high WebP through `CardImage`; invalid or missing assets fall back honestly. The visible disclaimer names Pokémon, Nintendo, TCGdex, Cardmarket, and listed companies. Third-party artwork rights are not independently proven, and broader reuse/self-hosting remains unapproved. Card detail now exposes a side-neutral `Add to a trade` CTA with strict local return/pick reconstruction. The surface brief is `.impeccable/surfaces/lib-tcg-cheap-web-live-card-detail-live-ex.md`.

## Product-owner correction and current state

The newest 2026-08-08 owner report identified the former autocomplete focus-loss path; that blocker is implemented and current. The shared external hook preserves query, input node, focus, caret/selection, and composition across Home and Trade. Stable `card-option-UUID` stream IDs, direct option semantics, one active/`aria-selected` option, wrapping keyboard navigation, exact Enter selection, Escape close, validated touch/click selection, and query-specific live status are implemented. The bounded listbox scrolls internally at `min(45svh, 32rem)` and keeps the active option visible without moving page/input scroll. Provider calls remain outside request paths.

The 2026-08-08 product-owner correction superseded the prior Home wording density and technical presentation. The two top-level customer features remain sealed product price comparison and singles price comparison; trade composition and difference calculation belong inside Singles. Colors, fonts, and the warm square visual direction remain approved. The implemented trade foundation is real but incomplete: NBP EUR/PLN conversion and explicit share/copy controls remain.

The 2026-08-08 minimal Home correction remains the current presentation baseline, now with usable shared autocomplete. Its exact-printing identity, accessibility, legal/methodology honesty, collapsed required caveats, local-only stream behavior, approved visual direction, plain states, and clear CTA remain requirements. The Phase 3 URL-backed trade foundation is implemented inside Singles with a side-neutral CardDetail CTA and strict return flow; NBP/PLN and explicit share/copy remain. Sealed remains unavailable.

## Validation and limitations

Validation evidence for the completed correction and trade foundation: canonical `direnv exec . mix check --verbose` passed with 222 tests, 0 Dialyzer errors, no Credo issues, and Ash codegen, Sobelow, format, compile, xref, unused-dependency, assets, and static checks passing; acquisition regression tests are included. Focused acquisition/trade/valuation/composition/bulk/Home/CardDetail coverage passed with 76 tests, and all web tests passed with 46 tests; intended-diff checks and browser validation passed. Final read-only code review was clean to commit with no findings after deterministic search-error/live-region component coverage was added. Browser validation preserved prefixes, node, focus, caret, and mid-string insertion; composition searched only after compositionend; ten-option keyboard navigation kept one selection and internal listbox scroll; Enter used the exact active ID; Escape retained query/focus; trade URL operations, CardDetail pick/return, stale fallback, 390px layout, 44px controls, and no-console behavior also passed. Detector findings were only two known explicit-square 3px border false positives plus advisories. The unresolved live TCGdex set-list timeout remains.

## Local development

- PostgreSQL runs through the worktree's Compose project on loopback port 5436 by default (`DB_PORT` can override it).
- Phoenix development server runs on port 4004.
- Worktrunk provides isolated parallel worktrees and port/environment separation.
- The direct localhost workflow is current; Caddy/domain integration is deferred.
- Development lifecycle is managed with `mix dev.up` and `mix dev.down`; non-main worktree identities include a short hash of the original branch to prevent sanitized-name collisions. `dev.up` verifies the expected Compose `postgres` service, restarts its exact Phoenix tmux session, and requires a 2xx HTTP response before reporting readiness.
- Theme initialization is bundled in the self-hosted JavaScript asset rather than emitted as a raw inline script. Browser CSP explicitly allows self-hosted scripts/connect/images and self plus inline styles required by LiveView runtime behavior.

The importer/sync boundary has fixture-backed deterministic coverage for provider errors, retries, idempotency, rollback, stale-response rejection, mapping review, assets, IDs, database constraints, set enumeration, brief validation, count mismatch, preservation, cross-set conflicts, TCG Pocket exclusion, and aggregate failure reporting. Focused search coverage passed 14 tests; SQL/Elixir Unicode parity and live PostgreSQL `EXPLAIN` index usage were verified. Caddy integration and CI remain deferred. Third-party artwork rights are not independently proven (and broader reuse/self-hosting is not approved), while UTC daily grouping with Europe/Warsaw presentation and full-catalogue validation remain explicit limitations.

Card detail and bounded valuation history are complete for this local-first surface. The shared autocomplete blocker and the URL-only EUR trade foundation are complete and validated. Sealed, admin, alias modeling, immutable catalogue-observation history, production operational tracking/budgets, full scheduling, NBP/PLN conversion, and explicit share/copy remain unfinished; ranking needs real full-catalogue tuning. No provider request runs in public paths. Remaining trade work is NBP/PLN and share/copy, followed by broader roadmap foundations.

## See Also

## Phase 3 trade foundation — implemented, not complete

The public `/trade` route is implemented inside Singles as a URL-only, mobile-first decision bench. It safely parses deterministic `left=id:qty,...&right=id:qty,...` state, merges/caps duplicates, sorts canonically, restores through `handle_params`, and updates every add/increment/decrement/remove through `push_patch`. It uses one local search and explicit side actions; untrusted events cannot inject rows. A bounded Ash read preloads up to 100 cards with their sets and current `tcgdex_cardmarket_v1` valuations, avoiding N+1 reads.

`TcgCheap.Trades.Valuation` is pure Decimal EUR unit/row/side/difference calculation. Stale values count, unknown/unvalued rows remain removable but unpriced, and incomplete totals/differences are marked. Missing or stale compositions render immediately and use `ValuationAcquisition.subscribe_and_request_many` for one canonical bulk re-read plus unique Oban jobs, with subscription before enqueue and no request-path HTTP. PubSub completion bulk-reloads; failures retain stale estimates.

The shared external `CardAutocomplete` hook serves Home and Trade. CardDetail has a side-neutral pending-pick CTA and strict local `/trade` return reconstruction. The trade surface has symmetric desktop ledgers, stacked mobile sides, square 44px controls, exact identity, relative update labels, acquisition states, and no 390px overflow. NBP EUR/PLN rate/resource/acquisition/display and explicit share/copy UI remain unfinished; the deterministic URL is only manually copyable. Sealed, auth, server trade persistence, snapshots, and provider HTTP in public paths remain unavailable.

Validation: focused acquisition/trade/valuation/composition/bulk/Home/CardDetail coverage passed with 76 tests, all web tests passed with 46 tests, warnings-as-errors compilation/assets/`mix check --no-test`/intended-diff checks passed, and browser checks covered focus/caret/composition, URL operations, pending pick, 390px layout, 44px controls, stale failure fallback, and no console errors. Canonical `direnv exec . mix check --verbose` passed with 222 tests, zero Dialyzer errors, no Credo issues, and Ash codegen, Sobelow, format, compile, xref, unused-dependency, assets, and static checks passing. Final read-only code review was clean to commit with no findings after deterministic search-error/live-region component coverage was added.

- [Detailed MVP Implementation Plan](../product/mvp-implementation-plan.md)
- [Reference Project Conventions](reference-project-conventions.md)
- [Provider and Acquisition Feasibility](provider-acquisition-feasibility.md)
